## 1. Write `rules/development-workflow.md` version 3

- [x] 1.1 Bump the frontmatter to `version: 3`. Keep `kind: standing-constraint`.
- [x] 1.2 Name the fragment's unit of work before any rule about it — a change, carried from proposal to record, living in three stages — and state what falls outside it: work too small to be a change skips `plan` and does not skip `ship`, since nothing reaches production except by merging. Then state the fragment's assumptions in its opening: a remote, pull requests, continuous integration, a deploy triggered by merging to the trunk, several concurrent sessions, and specification tooling. Do not name the forge as a separate assumption — pull requests imply it, and teardown's merge clause states what it must report. State no conditional for their absence, and no account of what another project shape would need — the brevity rules forbid it.
- [x] 1.3 Keep the existing sections unchanged except where a task below names them: spec-driven development and spec review, test design before implementation, implementation and execution, verification before any completion claim, independent review before completion, small reviewable commits, incremental development and scope control, requirements and assumptions, the repository is the source of truth.
- [x] 1.4 Add the change's branch and working tree: a branch cut from the freshly fetched trunk and a working tree of its own at the fixed path `.worktrees/`, with `.claude/worktrees/` named beneath it as the Claude Code binding rather than in the rule sentence. Task 1.12 states the invariant these sit under; do not state it here as well. Do not carry the scope exclusion for a tooling-created tree: keying every rule to the change puts such a tree outside them without one, and the capability forbids stating it. Do not carry the ignore entry or the scoping of recursive tools; both are one-time setup obligations, recorded per task 4.6.
- [x] 1.5 Add provisioning: tracked files only, the false pass, completeness as an end state, the shared-service namespace brought to the project's initial state, provisioning as a state to reach rather than an act performed, and checking for a service before declaring it absent.
- [x] 1.6 Add the namespace rule service-neutrally, with the reclamation gap named inline.
- [x] 1.7 Add the report: the fixed row and its cells, on entering and again before stopping, derived from the repository, with the verification cell composed against provisioning.
- [x] 1.8 Add the stage vocabulary: the transitions as the closed set, the derivation rule, four examples, the two states whose derivation reorders (`plan:tests-derived`, `ship:pr-open`) and the off-path names. Do not enumerate every derived state. State that the family prefix is always written.
- [x] 1.9 Add the one record that gates a decision: the production-confirmation waiver, at the delivery sequence, written to the change's own artifacts. No ledger, no stage-derivability apparatus, and no review-verdict record — the commit holding the approved plan is already the marker that a verdict permitted proceeding.
- [x] 1.10 Add delivery: the code-review loop bounded at three; the work's pull request; merge and deploy confirmed by the operator; the production-confirmation gate and the obligation to propose how the effect is observed; the record through the last pull request, with two stated as a floor and a remedial cycle carrying its own; the exemption for a change with no externally observable effect and the recorded operator waiver it rests on; the unhealthy-deploy and absent-effect paths; and that the pipeline is the only path to production. State the archive step as a role and name no command beneath it: every other step leaves the tool to the project, and task 1.2 already puts the specification tooling among the fragment's assumptions. Name the outcomes that exit the review loop rather than consume a round, alongside its bound of three.
- [x] 1.11 State both terminals of the absent-effect path: a defect re-enters at the review gate, and the wrong change is the confirmation gate's second waivable class — observation made, effect absent, not to be corrected in place — waived and recorded like the first, the waiver naming a successor's queue entry or the branch it was opened on, where one is intended. State that a recorded waiver completes `confirm`, so `ship:confirmed` is true of a waived change — the transition defined against both ways of passing the gate, not a state asserted without its transition.
- [x] 1.12 State the branch invariant as the change's, not the session's: one change, one branch, one working tree, kept from proposal to record; sessions on one change run one after another and never concurrently; the branch is brought back to the freshly fetched trunk after each merge and rebased onto it periodically in between. Scope fetching, rebasing and merging separately and give a route for each part of the change's life: fetch throughout; before the branch is pushed, bring the trunk in by rebasing; after, by merging — a rebase then needs a force push over a branch under review where a merge does not. A rule that stops at the push leaves a session no way to stay current through `ship`, which is where a change waits longest.
- [x] 1.13 Add teardown: the observable gate, both routes, which clauses the abandonment record displaces, committing the record first, the execution context, and that the namespace is not covered. State that it covers the change's one branch, locally and on the remote; that the abandonment record displaces both merge conjuncts and the unpushed clause; that the clause names the record's own pull request as its own conjunct, so a merged work pull request does not pass the gate at `ship:merged` and an abandoned change that opened none does not pass it vacuously; and that the clause reads a pull request's merged state rather than branch ancestry, so a squash- or rebase-merging forge does not make every branch permanently unremovable.
- [x] 1.14 Add the change queue: one change per session, the dependent and independent paths, `docs/change-queue.md` and how it differs from `docs/deferred-work.md`, and the seam with the scope rule already in the fragment.
- [x] 1.15 State the handoff: an identified change is opened with a `handoff.md` at its root and no proposal — why it was identified, what bears on it, what it must not undo — and the session that takes it up writes the proposal. State that a commit on that branch is proposed as soon as it is opened, and that where the commit is declined the session says the handoff is unsaved rather than continuing. State that such a branch takes no working tree and does not become the branch the session works on.
- [x] 1.16 Do not add the four setup rules to the fragment; record them in `docs/change-queue.md` for the capability that owns project setup, per task 4.5, and state in the fragment's own capability that a one-time obligation is recorded rather than added.
- [x] 1.17 Amend the commit rules, which the fragment already carries: state what happens to a commit made while applying. The derived tests fail by design until the implementation is complete, so the verification the commit rule requires cannot pass for the whole of `build:applying` and a hook running it blocks every commit. Name the bypass — `--no-verify` — rather than describing one, say the failure is expected rather than a defect, and say the bypass suspends the pre-commit check and not the gate: verification still runs before any completion claim.
- [x] 1.18 Read both fragments for references that only resolve inside this library — no `rules/` path, no sibling fragment, no import. The rule binds the binding fragment as well as the workflow one, and v3's reference to the binding is conditioned on the project carrying it.

## 2. Write `rules/development-workflow-database.md`

- [x] 2.1 Create it with `kind: standing-constraint` and `version: 1`.
- [x] 2.2 State the affordance first: a real database runs in a container, integration tests run against it and not against a double or a lighter engine, and the running containers are checked before concluding it is unavailable.
- [x] 2.3 Then the binding: a database per working tree named from it, never the development database or another session's; the environment file a working tree does not inherit; migrating is not seeding; and reading a failing test's assertion message before calling a failure pre-existing.
- [x] 2.4 Confirm v3 refers to this fragment as an adjacent section of the same conventions file, never by a `rules/` path, so a project holding v3 alone can still learn a binding exists.
- [x] 2.5 Check it restates no obligation the workflow fragment already states — it names the service and the operations that reach the stated end state, and nothing else.

## 3. Remove the three session fragments

- [x] 3.1 Delete `rules/worktree-isolation.md`, `rules/change-delivery.md`, `rules/deferred-work.md`.
- [x] 3.2 Leave the two-path account of how a fragment reaches a project unchanged in both `rules/README.md` and `AGENTS.md`. An earlier pass added a third path for the database binding fragment and it was reverted: `toolkit-structure` fixes the count at two and this change ships no delta against it, and the justification was independently false — the `@` import reaches a binding fragment like any other, so what the binding lacks until the tooling change is a *tool* route, not a route.
- [x] 3.3 Grep the repository for references to all three and repair each — `docs/deferred-work.md`, `AGENTS.md`, `openspec/changes/add-namespace-reclamation/proposal.md`, any skill or agent naming them.

## 4. Seed this repository's own artifacts

- [x] 4.1 Carry no gate log. An earlier draft mandated one, and this change kept its own as a demonstration of the artifact. With no ledger required and no verdict record obliged, it is a file nothing reads and a second place for a decision to be stated — which is how `design.md` and it came to disagree about how many records survive. Delete `gate-log.md`; keep in `design.md` the reversals that changed a decision, and in `docs/change-queue.md` the follow-up work a round identified.
- [x] 4.2 Create `docs/change-queue.md`, stating what it holds, when an entry is deleted, and how it differs from `docs/deferred-work.md`.
- [x] 4.3 Add the entry for `add-session-workflow-tooling`, naming this change as the one that argued it.
- [x] 4.4 Add a cross-reference from `docs/deferred-work.md` to the new file, so a reader who reaches the wrong one is redirected.
- [x] 4.5 Record the four setup rules task 1.16 keeps out of the fragment as `move-setup-gates-to-project-setup`, stating for each why it is a one-time act a session never performs, and which of them `project-foundation` already owns.
- [x] 4.6 Record the two containment obligations the fragment does not carry — naming the working-tree roots in the project's ignore file, and scoping every recursive tool that does not read it — as `seed-the-worktree-ignore-entry`, for the capability that owns project setup. The requirement obliging the fragment to omit them asserts they are recorded, so nothing else in this change makes that true. State the gap the entry closes against v3 as it ships, not against the draft that carried the obligation in the fragment. Carry into the entry the property the deleted scenario asserted — that a tool walking the repository from its root is either scoped explicitly or excluded by the ignore file, and collects, lints, type-checks or packages no other session's copy of the project — so it travels to the capability that will own it rather than being lost with the scenario.

## 5. Update the suite

- [x] 5.1 After task 8.1, which is what creates the name: dispatch `ai-toolkit:change-test-writer` against the committed, reviewed delta specs — test command `bash tests/run.sh`, test-path glob `tests/cases/**`. The exemption recorded in `tests/README.md` does not apply here.
- [x] 5.2 Confirm the manifest accounts for the cases this change implicates: `session-fragments-name-fixed-paths.sh`, `session-fragments-name-no-sibling.sh`, `session-fragment-frontmatter.sh`, `session-fragments-not-inlined.sh`, `deferred-work-states-workflow-seam.sh` and `change-delivery-names-no-working-tree.sh` all assert fragments this change deletes; `inlined-body-matches-fragment.sh`, `managed-block-current-version.sh` and `managed-block-older-version.sh` are coupled to the workflow fragment's version.
- [x] 5.3 Rewrite `tests/coverage.md`'s `session-workflow` section against this change's delta scenarios, and delete the rows for the cases the manifest marks obsolete.
- [x] 5.4 Apply the manifest's obsolete-test entries. Do not weaken or delete a case to reach green.

## 6. Verify

- [x] 6.1 `openspec validate consolidate-development-workflow --strict`.
- [x] 6.2 `bash tests/run.sh` — full suite green.
- [x] 6.3 Walk the transitions the fragment states: confirm each names a distinct state a reader can tell from its neighbours. This is a check on the vocabulary, not on the repository — nothing requires that every stage be derivable, and an audit for a trace per stage is the apparatus decision 6 withdrew.
- [x] 6.4 Read v3 end to end as a project would see it — inlined, with no fragment files in sight — and check that no rule arrives after the point it applies.
- [x] 6.5 Confirm nothing in the repository still names a deleted fragment.
- [x] 6.6 Walk the new sections against the retargeted rule that a rule states an action and a defect appears only as its reason — each new rule's durable half is its instruction and its perishable half its consequence.
- [x] 6.7 Walk the vocabulary section against what the capability requires of it: the transitions present as the closed set, the derivation rule, the two states whose derivation reorders, the off-path names, and the always-written family prefix — and no enumeration of derived states beside the rule that generates them.
- [x] 6.8 Walk v3 against the four `project-bootstrap` requirements governing it — role before tool, the ordered sequence with checkable preconditions, the bounded dispatch loop with its exit outcomes, and the version increment. Nothing else in this change checks the file that capability governs. The expected answer on the ordered-sequence requirement: the implementation gate still names the review verdict, the committed plan and the derived tests as its preconditions — the commit is how the verdict is observed, and removing its separate record does not remove it from the gate. Record the result in `design.md`, where this change's other reconciliations against that capability sit; there is no gate log to carry it.

## 7. Review and deliver

- [x] 7.1 `/code-review` over the diff. Apply the findings; re-review only where the fixes were substantial. Bound at three.
- [ ] 7.2 Commit, push, open the pull request, and let CI run.
- [x] 7.3 Establish whether a delta can carry the `## Purpose` change. **It cannot, and it fails silently.** A `## Purpose` block added to a delta for a *modified* capability passes `openspec validate --strict` and is parsed into no delta operation — `openspec show --deltas-only` reports the same requirement deltas with or without it, unchanged in count and content. The artifact instructions say purpose belongs to new capabilities only; what the probe adds is that the non-carrying case is silent rather than rejected, so a change that tried it would archive with the purpose unchanged and nothing reporting why.
- [ ] 7.4 Make the `## Purpose` edit by hand in the archive commit, where the deltas are applied and no window of inconsistency opens. `openspec/specs/session-workflow/spec.md`'s purpose is false on three counts: it describes the capability as governing three separate fragments, it says `project-bootstrap` owns the inlined workflow fragment "which this capability does not touch", and it says the capability governs fragment content only. Then confirm the rewritten purpose is true of the archived capability.

## 8. Align the vocabulary with OpenSpec's

- [x] 8.1 Rename `agents/openspec-change-reviewer.md` to `agents/change-plan-reviewer.md` and `agents/openspec-test-writer.md` to `agents/change-test-writer.md`, with their `.checks.yaml` fixtures, and update the `name:` in each frontmatter to match the filename.
- [x] 8.2 Rename `rules/test-manifest.md` to `rules/test-plan.md`, and repair every reference to it.
- [x] 8.3 Bring the recorded check fixtures to the new vocabulary: the planted instruction in `change-plan-reviewer.checks.yaml` demands a verdict from the renamed set, and `change-test-writer.checks.yaml` names `test-plan.md`. A fixture left in the superseded words tests a surface that no longer exists.
- [x] 8.4 Write a `MODIFIED` delta for **every** requirement under `openspec/specs/change-test-authoring/` naming the artifact and **every** requirement under `openspec/specs/change-review/` naming a verdict — not only the ones whose subject is the name. A partial rename leaves each capability contradicting itself about its own artifact and its own verdict set, and `openspec validate --strict` does not report it.
- [x] 8.5 Re-run both agents' recorded checks and re-record their fixtures from those runs. The renames edit each agent's `name`, `description` and body, which `agent-authoring` says invalidates the trigger check and the cold-run check for both; task 8.3 rewrote the fixture payloads, so until the checks run against them the fixtures record inputs no run has used, and that capability forbids reporting an agent complete in that state. Run the trigger checks first — they gate the cold runs — against an evaluator holding every library asset and every asset committed under `.claude/`, derived by glob. Then run each cold-run check per its recorded recipe, isolating `change-test-writer`'s in a worktree since its grant includes `Write` and `Bash`. Re-run too the trigger check of any asset whose expected routing names either agent.
- [x] 8.6 Grep the repository for the superseded names — `test-manifest`, `openspec-change-reviewer`, `openspec-test-writer`, `PROCEED`, `PROCEED WITH CHANGES`, `CHANGES REQUIRED`, `REJECT` — and confirm every remaining hit is either a deliberate historical reference or inside the archive.
