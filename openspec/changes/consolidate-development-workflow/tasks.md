## 1. Write `rules/development-workflow.md` version 3

- [ ] 1.1 Bump the frontmatter to `version: 3`. Keep `kind: standing-constraint`.
- [ ] 1.2 State the fragment's assumptions in its opening: a remote, a forge that reports a pull request's merged state, continuous integration, a deploy triggered by merging to the trunk, several concurrent sessions, and specification tooling. State that a project of another shape is served by a publication of its own, not by conditionals here.
- [ ] 1.3 Keep the existing sections unchanged except where a task below names them: spec-driven development and spec review, test design before implementation, implementation and execution, verification before any completion claim, independent review before completion, small reviewable commits, incremental development and scope control, requirements and assumptions, the repository is the source of truth.
- [ ] 1.4 Add the session's branch and working tree: the scope exclusion for a tooling-created tree, a tree and branch of its own from the freshly fetched trunk, the note that the rule governs the branch worked on at any moment and not the number a change accumulates, `.claude/worktrees/`, the ignore entry, and scoping every recursive tool that does not read it.
- [ ] 1.5 Add provisioning: tracked files only, the false pass, completeness as an end state, the shared-service namespace brought to the project's initial state, provisioning as a state to reach rather than an act performed, and checking for a service before declaring it absent.
- [ ] 1.6 Add the namespace rule service-neutrally, with the reclamation gap named inline.
- [ ] 1.7 Add the report: the fixed row and its cells, on entering and again before stopping, derived from the repository, with the verification cell composed against provisioning.
- [ ] 1.8 Add the stage vocabulary — `plan:` draft/review/revise/approved/committed/tests, `build:` doing/verify/review/fix/clear, `ship:` pr/merged/deployed/confirmed/record/done, plus `blocked:<what>` and `abandoned`. Mark every one of the nineteen names transient or persistent, leaving none unclassified, and for each persistent one name its trace — splitting them three ways: read from git or the forge (`plan:committed`, `ship:pr`, `ship:merged`, `ship:record`, `ship:done`), read from `test-manifest.md` (`plan:tests`), and carried by `gate-log.md` (the rest).
- [ ] 1.9 Add the recording rules, all writing to `gate-log.md` at the change's root: a review dispatch recorded when made, the verdict it returns, what was done about that verdict, each operator confirmation of a merge / a deploy's health / a production effect, a production-confirmation waiver, an abandonment record, a block naming what it waits on, each branch as it is cut, and each pull request as it is opened. Place them at the two review sections, at the delivery sequence, at the branch-topology section, at teardown, and wherever a session stops blocked. State that an entry is committed with the next commit the session proposes where one is coming in that session, and on its own where none is — naming the four cases: every post-merge entry, the record's own pull request, an abandonment record, and a block recorded by a session that is ending. State that the closed list of what a stage's trace needs does not bound what the file holds.
- [ ] 1.10 Add delivery: the code-review loop bounded at three; the work's pull request; merge and deploy confirmed by the operator; the production-confirmation gate and the obligation to propose how the effect is observed; the record through the last pull request, with two stated as a floor and a remedial cycle carrying its own; the exemption for a change with no externally observable effect and the recorded operator waiver it rests on; the unhealthy-deploy and absent-effect paths; and that the pipeline is the only path to production. State the archive step as a role with the specification tooling named beneath it, never in the role sentence. Name the outcomes that exit the review loop rather than consume a round, alongside its bound of three. Record each branch in `gate-log.md` as it is cut — or, for the change's first branch, when the change's root is created, since the branch precedes the file — and each pull request as it is opened, naming its branch.
- [ ] 1.11 State both terminals of the absent-effect path: a defect re-enters at the review gate, and the wrong change is the confirmation gate's second waivable class — observation made, effect absent, not to be corrected in place — waived and recorded like the first, the waiver naming a successor's queue entry or proposal branch where one is intended. State that a recorded waiver satisfies `ship:confirmed` for reporting.
- [ ] 1.12 Add the branch topology as its own section: the branch set is the branches carrying the change's own work (work, remedial, record) and excludes a proposal-only branch, which is recorded as its own change's first branch instead, every branch in it is cut from the freshly fetched trunk, the record's branch carries the operator confirmations, any waiver and the archive commit while a remedial cycle keeps its own gate records on its own branch, the record's branch becomes the session's working branch, and why cutting from a merged work branch's tip fails under a squash- or rebase-merging forge.
- [ ] 1.13 Add teardown: the observable gate, both routes, which clauses the abandonment record displaces, committing the record first, the execution context, and that the namespace is not covered. State that it covers every branch in the change's own set — work, remedial, record — each checked on its own pull request and enumerated from `gate-log.md`; that the abandonment record displaces both merge conjuncts and the unpushed clause, and that its subject is every branch the change created while it is committed on whichever the session holds; that the clause names the record's own pull request as its own conjunct, so a merged work pull request does not pass the gate at `ship:merged` and an abandoned change that opened none does not pass it vacuously; and that the clause reads a pull request's merged state rather than branch ancestry, so a squash- or rebase-merging forge does not make every branch permanently unremovable.
- [ ] 1.14 Add the change queue: one change per session, the dependent and independent paths, `docs/change-queue.md` and how it differs from `docs/deferred-work.md`, the proposal-only branch, and the seam with the scope rule already in the fragment.
- [ ] 1.15 Add the gates-that-did-not-run rules, each phrased as an obligation the fragment places on the project rather than as a rule addressed to the project directly: continuous integration fails rather than skips on a dependency the verification cannot reach — scoped to that cause, not to every skip — a local hook is not an authority, secrets are excluded by the ignore file rather than by vigilance, and the project states its test command and test-path glob.
- [ ] 1.16 Read both fragments for references that only resolve inside this library — no `rules/` path, no sibling fragment, no import. The rule binds the binding fragment as well as the workflow one, and v3's reference to the binding is conditioned on the project carrying it.

## 2. Write `rules/development-workflow-database.md`

- [ ] 2.1 Create it with `kind: standing-constraint` and `version: 1`.
- [ ] 2.2 State the affordance first: a real database runs in a container, integration tests run against it and not against a double or a lighter engine, and the running containers are checked before concluding it is unavailable.
- [ ] 2.3 Then the binding: a database per working tree named from it, never the development database or another session's; the environment file a working tree does not inherit; migrating is not seeding; and reading a failing test's assertion message before calling a failure pre-existing.
- [ ] 2.4 Confirm v3 refers to this fragment as an adjacent section of the same conventions file, never by a `rules/` path, so a project holding v3 alone can still learn a binding exists.
- [ ] 2.5 Check it restates no obligation the workflow fragment already states — it names the service and the operations that reach the stated end state, and nothing else.

## 3. Remove the three session fragments

- [ ] 3.1 Delete `rules/worktree-isolation.md`, `rules/change-delivery.md`, `rules/deferred-work.md`.
- [ ] 3.2 Rewrite the two-path account of how a fragment reaches a project in both `rules/README.md` and `AGENTS.md`, which offers an `@` import or a shipped tool and covers neither route the database binding fragment takes — hand-pasted now, a second managed block after the tooling change.
- [ ] 3.3 Grep the repository for references to all three and repair each — `docs/deferred-work.md`, `AGENTS.md`, `openspec/changes/add-namespace-reclamation/proposal.md`, any skill or agent naming them.

## 4. Seed this repository's own artifacts

- [ ] 4.1 Create this change's own `gate-log.md`, carrying the review rounds already run. This demonstrates the artifact rather than adopting it: this repository carries no managed workflow block and is not a consumer of the fragment, so no obligation follows for the next change here.
- [ ] 4.2 Create `docs/change-queue.md`, stating what it holds, when an entry is deleted, and how it differs from `docs/deferred-work.md`.
- [ ] 4.3 Add the entry for `add-session-workflow-tooling`, naming this change as the one that argued it.
- [ ] 4.4 Add a cross-reference from `docs/deferred-work.md` to the new file, so a reader who reaches the wrong one is redirected.

## 5. Update the suite

- [ ] 5.1 Dispatch `ai-toolkit:openspec-test-writer` against the committed, reviewed delta specs — test command `bash tests/run.sh`, test-path glob `tests/cases/**`. The exemption recorded in `tests/README.md` does not apply here.
- [ ] 5.2 Confirm the manifest accounts for the cases this change implicates: `session-fragments-name-fixed-paths.sh`, `session-fragments-name-no-sibling.sh`, `session-fragment-frontmatter.sh`, `session-fragments-not-inlined.sh`, `deferred-work-states-workflow-seam.sh` and `change-delivery-names-no-working-tree.sh` all assert fragments this change deletes; `inlined-body-matches-fragment.sh`, `managed-block-current-version.sh` and `managed-block-older-version.sh` are coupled to the workflow fragment's version.
- [ ] 5.3 Rewrite `tests/coverage.md`'s `session-workflow` section against this change's delta scenarios, and delete the rows for the cases the manifest marks obsolete.
- [ ] 5.4 Apply the manifest's obsolete-test entries. Do not weaken or delete a case to reach green.

## 6. Verify

- [ ] 6.1 `openspec validate consolidate-development-workflow --strict`.
- [ ] 6.2 `bash tests/run.sh` — full suite green.
- [ ] 6.3 Walk the stage vocabulary against the repository: for each stage, confirm something readable distinguishes it from its neighbours.
- [ ] 6.4 Read v3 end to end as a project would see it — inlined, with no fragment files in sight — and check that no rule arrives after the point it applies.
- [ ] 6.5 Confirm nothing in the repository still names a deleted fragment.
- [ ] 6.6 Walk the new sections against the retargeted rule that a rule states an action and a defect appears only as its reason — each new rule's durable half is its instruction and its perishable half its consequence.
- [ ] 6.7 Walk all nineteen stage names: confirm each is classified, that every persistent one has a named trace, and that the fragment claims no transient one is recoverable.
- [ ] 6.8 Walk v3 against the four `project-bootstrap` requirements governing it — role before tool, the ordered sequence with checkable preconditions, the bounded dispatch loop with its exit outcomes, and the version increment — and record the result in this change's `gate-log.md`. Nothing else in this change checks the file that capability governs.

## 7. Review and deliver

- [ ] 7.1 `/code-review` over the diff. Apply the findings; re-review only where the fixes were substantial. Bound at three.
- [ ] 7.2 Commit, push, open the pull request, and let CI run.
- [ ] 7.3 Update `openspec/specs/session-workflow/spec.md`'s `## Purpose`, which is false on three counts after this change: it describes the capability as governing three separate fragments, it states that `project-bootstrap` owns the inlined workflow fragment "which this capability does not touch", and it says the capability governs fragment content only. First establish whether a delta can carry the change; only if it cannot, make it by hand in the archive commit, where the deltas are applied and no window of inconsistency opens. Then confirm the rewritten Purpose is true of the archived capability.
