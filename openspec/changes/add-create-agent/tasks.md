## 1. Reconcile the frontmatter contract and establish the harness facts

Tasks 1.6, 1.7, 1.9, and 1.10 are not frontmatter work — they settle harness behaviour that decides how requirements in sections 2 and 4 are worded, and must run before those are written. All four ride on the same `--plugin-dir` load of one throwaway agent.

- [ ] 1.1 Re-read `plugin-dev/skills/agent-development/scripts/validate-agent.sh` and record the fields it treats as required, the constraints it enforces on each, and the checks it emits as warnings. Record the **value** constraints as well as the presence checks — the `name` pattern and length bounds it errors on, the `color` set and the `model` set it warns outside of, and the system-prompt length it warns above — since the contract's claim that a conforming agent produces exactly one warning is only true subject to all of them, and an earlier draft collected the presence checks alone
- [ ] 1.2 Re-read `plugin-dev/skills/agent-development/SKILL.md` and record where it disagrees with the validator, in particular the prose trigger summary versus `<example>` blocks
- [ ] 1.3 Re-count the shipped agent corpus for `model`, `color`, `tools`, and `effort` usage, and for prose versus `<example>` descriptions, so the reconciliation table carries current numbers. Count the 31 agents at `<plugin>/agents/*.md` and state the exclusion of the three nested under `skill-creator/skills/skill-creator/agents/`, since mixing the two denominators is what produced the wrong figures in the first draft. Record the **values** observed for `effort`, not only how many agents declare it — the corpus is the only source there is for that field, so a count alone leaves the contract with nothing to say about what may be written in it
- [ ] 1.4 Record both `tools` syntaxes and collect the scoped-grant examples (`Agent(...)`, `Workflow(...)`) from the corpus
- [ ] 1.5 Record the description-opener conflict as its own row — the validator warns without `use this agent when`, `agent-development/SKILL.md` templates it, 10 of 31 agents use it — and resolve it as permitted per `design.md` D4
- [ ] 1.6 Establish empirically, on a throwaway agent, both halves of what a real dispatch requires: that an uncommitted `agents/<name>.md` is dispatchable by name via `claude -p --plugin-dir <repo>`, **and** that the dispatched agent's own report is recoverable verbatim rather than only as the top-level session's summary of it (try `--output-format stream-json` carrying subagent messages, or a dispatch instructed to relay the report unaltered). If `--plugin-dir` fails either half, try `claude --agents <json>` before recording the real-dispatch form as unavailable — `design.md` D7 names both mechanisms, and falling back to simulation on the failure of one retires the preferred form on partial evidence. Record both results — together they decide whether the cold-run check's live half is written around a real dispatch or around the simulation fallback, and a form that registers the agent but cannot surface its report counts as unavailable. On the same throwaway agent, give the frontmatter `name` a value deliberately different from the file's base name and record which one the agent is invoked by. The corpus has no mismatch and `agents/README.txt` only shows the file name used when no `name` is declared, so nothing established so far decides precedence; the flat-file rule requires agreement either way, but its rationale should not assert a precedence the repository has never observed
- [ ] 1.7 Establish whether a companion `agents/<name>.checks.yaml` is ignored by agent discovery, confirming the `.md`-only behaviour `agents/README.txt` records for placeholders holds for companion files too
- [ ] 1.8 Write the reconciliation as a dated snapshot naming all three sources and the points on which they conflict
- [ ] 1.9 Establish whether an agent declaring `metadata.tags` **loads**, on the same throwaway agent and `--plugin-dir` load as 1.6. The validator half needs no experiment and is settled by inspection: `validate-agent.sh` greps for `name`, `description`, `model`, `color`, and `tools` individually and never enumerates permitted keys, so any additional key passes it — unlike the skill schema, whose `quick_validate.py` rejects unknown top-level fields. Record that reading, then run the loader half only. No agent in the corpus declares `metadata`, so the repository's own tags convention has no external evidence behind it for this asset type. If the load fails, record the finding, rule tags out for agents rather than writing an unverified field into the contract, **and correct the `agent-authoring` delta accordingly** — it currently states that `metadata.tags` MAY be declared. Remove the throwaway agent either way
- [ ] 1.10 Establish that worktree isolation composes with a real dispatch, since `design.md` D7 requires both of a write-capable agent and neither draft checked that they work together. On the same throwaway agent: create a git worktree, copy the uncommitted `agents/<name>.md` into it, run `claude -p --plugin-dir <worktree>` from inside the worktree, and record whether the agent registers and dispatches by name. Record also what happens **without** the copy, which is the procedure as an earlier draft stated it — a worktree is created from a committed ref and the drafted agent is untracked, so the expectation is that nothing is registered and the dispatch has no target. If the copy proves insufficient (a plugin root needs more than the one file), record what else had to be carried in, since that is what the requirement has to name. Remove the worktree afterwards

## 2. Author `create-agent` using `create-skill`

- [ ] 2.1 Run `create-skill`'s Intent checkpoint — name `create-agent`, confirm the name is unused under `skills/`, choose tags from the vocabulary already in use, state purpose and triggering conditions, and confirm the target is this repository's `skills/`
- [ ] 2.2 Run the Shape checkpoint — decide flat versus bundled against the threshold in `design.md` D10, defaulting to `SKILL.md` plus `references/frontmatter-contract.md` and `references/cold-run-check.md`, and collapsing to flat if the body proves readable in one pass
- [ ] 2.3 Run the Draft checkpoint — present the proposed frontmatter and a body outline for approval; write no file before approval
- [ ] 2.4 Write the routing gate as the first section of the body, routing to `create-skill`, slash commands, and `rules/`, and stating that a referral is a complete outcome
- [ ] 2.5 Write the frontmatter contract section from task 1.8, including the direction to declare `model` and `color` for validator compatibility with the reason stated, `model: inherit` as the default value so that compatibility pins no model on a consuming project, `metadata.tags` as an optional field carrying the vocabulary rule `skill-authoring` applies to skills (subject to task 1.9's result), `effort` with the values task 1.3 observed and an explicit statement that neither published source documents the field and the accepted set is therefore unverified — no invented enumeration, since the Shape checkpoint asks the author to confirm this field and must not ask against silence — the value constraints task 1.1 recorded — the `name` pattern and length the validator errors on, and the `color` set it warns outside of — and the note that a conforming agent still emits the `<example>` warning by design, stated with the conditions that make it exactly one: a colour from the accepted set and a body under the length the same script warns at
- [ ] 2.6 Write the flat-file rule and its rationale about invocation names, including that a non-`.md` companion file may sit beside an agent, is not an asset, and needs no exception from `toolkit-structure` — that capability constrains asset placement and placeholder discoverability, not non-`.md` files. State the `name`/file-name rule as a requirement that the two agree, with task 1.6's finding as the reason if it settled precedence and as an open question if it did not; do not assert a precedence the corpus cannot support
- [ ] 2.7 Write the description standard — inherited criteria plus the prose trigger summary and the `## When to invoke` body section, with `<example>` blocks ruled out and why, and the `Use this agent when [conditions]` opener permitted with the distinction from user-addressing second person stated
- [ ] 2.7a Write the section stating that agents are authored into this repository's `agents/`, never into the project the workflow was invoked from, and carry that scope into `create-agent`'s own description
- [ ] 2.8 Write the tool-grant section — least privilege, both syntaxes, scoped grants, and omission as a deliberate choice
- [ ] 2.9 Write the system prompt standard covering all five required elements, with the dispatch contract first
- [ ] 2.10 Write the three checkpoints, with the routing gate evaluated at Intent
- [ ] 2.11 Write the post-write validation list, covering only what is knowable at write time, including that `name` satisfies the validator's format and length constraints and a declared `color` is drawn from its accepted set — both are hard-error-or-warning conditions the standard now records, and a validation list that checks name/filename agreement while ignoring whether the name is well-formed passes an agent the tool it targets rejects — and including that the `## When to invoke` section the description points at actually exists in the body — the description form this standard prescribes ends in that pointer, so without the check a conforming-looking agent ships a dangling reference and validation reports success, which is the failure `create-skill`'s bundled-resource check already covers for skills. State that the fixtures file is deliberately not among the items — the checks that produce its contents have not run yet, so requiring it here would fail a conforming agent and invite a stub file
- [ ] 2.12 Write the trigger check section, stating that it runs first, gates the cold-run check, and uses an evaluator holding every skill **and agent** in the library — the same composition `skill-authoring` requires, stated identically so the two capabilities cannot drift — and state the scope boundary explicitly: the evaluator holds this library and not other installed plugins, because which plugins are installed is machine-local and a check that varied with it would not be a statement about this library. Name the residual risk rather than implying the check covers it: `plugin-dev` ships both a skill and an agent for authoring agents, so a pass here is not a pass in a project that installs them
- [ ] 2.13 Write the cold-run check section — the live half's preferred real dispatch and simulation fallback per tasks 1.6 and 1.10, with every registration mechanism the standard records tried before the preferred form is declared unavailable, the requirement that the live half evaluate the agent's own report and that a form which cannot surface it verbatim counts as unavailable, the fallback's two limits (all tools granted; body delivered as a prompt rather than a system prompt), the static half as mandatory in both forms with the reason each way, declared-property pass criteria — with the two behavioural ones written as something a run can actually show: the dispatch contract is incomplete when the returned report evidences context the payload never supplied in any of three forms (the agent asked, declared itself blocked, or proceeded on an assumption not derivable from the payload), since neither live form is interactive and a criterion satisfied only by asking tests an event that cannot occur; and termination judged by *why* the run ended, a body-directed conclusion passing where a run cut off by the harness's turn or time budget fails as a missing stopping condition — the failure-to-revision mapping, and worktree isolation including the copy of the drafted definition into the worktree before the run (a worktree comes from a committed ref and the drafted agent is untracked, so an unpopulated worktree registers no agent and the preferred form cannot run at all — task 1.10's finding), the payload-path rewrite, the abort on a *writable* path outside the worktree with the reason the abort binds writes only — a read-only path outside it is legitimate, and a wider rule makes the check unsatisfiable for any agent that reads a plugin cache or a sibling checkout — the requirement that the run be **launched from the worktree root** with the authoring checkout unavailable as a writable directory — the payload rewrite alone closes the isolation only against a body that already honours absolute-path discipline, while a body using relative paths resolves them against the launch directory, so the defect the check exists to catch is the one that escapes it, and escapes it before the check can report anything — the mechanism per form (`isolation: "worktree"` for the simulation, which already sets the subagent's working directory to the isolated root; for a real dispatch, a worktree created first, the drafted definition copied into it, then entered and `--plugin-dir` run against the worktree from inside it), and what the isolation does *not* contain — it redirects path-addressed writes and is not a sandbox, so a shell grant, which is itself one of the triggers for isolation, can address any path the process can reach, and containment for such an agent rests on the permission mode the run is launched under. State that limit beside the network-tool one rather than claiming the run cannot mutate the authoring checkout
- [ ] 2.14 Write the section on which check a given edit invalidates, including that adding an asset invalidates competing checks
- [ ] 2.14a Write the fixtures section — what is recorded (trigger prompts with expected routing, cold-run payload), what is not (outcome, run date) and why a stale pass is misleading rather than merely stale, the `agents/<agent-name>.checks.yaml` location, the reason it is neither in frontmatter nor in the body — both sit on the dispatch path, one parsed at every load and one carried on every dispatch; **not** that frontmatter ships to consuming projects, since a companion file in `agents/` ships identically and the distinction is that it is never read into a context — that the extension rather than the depth is what keeps it out of discovery, and that recording the fixtures is a condition of reporting the agent complete — written after the checks run, since it records what they ran against
- [ ] 2.15 Verify every bundled reference the body links to exists

## 3. Wire the standard into the library

- [ ] 3.1 Add the `AGENTS.md` pointer to `skills/create-agent/SKILL.md` as the agent authoring standard, without restating its contents
- [ ] 3.2 Confirm `AGENTS.md` no longer leaves agent authoring as a dangling reference
- [ ] 3.3 Edit `skills/create-skill/SKILL.md` so its outward route names `create-agent` as the destination for work needing its own context
- [ ] 3.4 Edit `create-skill`'s trigger check section so the evaluator holds every skill **and agent** in the library, with the reason stated, and so it states the scope boundary in the same breath — this library, not other installed plugins, because installation is machine-local state a re-run cannot reproduce. The reason for widening across asset types does not stop at the library on its own, so leaving the edge unstated invites the next reader to keep going
- [ ] 3.5 Add the rule that adding an asset invalidates the recorded checks of assets it competes with
- [ ] 3.6 Add to `create-skill` the rule that a trigger check's fixtures are recorded and its outcomes are not, with the stale-result reasoning stated — `skill-authoring` requires outcomes to be *reported* but has never required them to be *recorded*, so the invalidation rule in 3.5 would otherwise dangle on a practice nothing mandates
- [ ] 3.7 Reconcile the companion-file rule with `openspec/specs/toolkit-structure/spec.md`. No permission needs adding — that capability constrains where *assets* are placed and requires a placeholder to be undiscoverable, and places no restriction on non-`.md` files — but its rationale clause states that *every* file directly inside `agents/` is read as an agent definition, which is false, makes its own placeholder requirement unsatisfiable, and contradicts the fact the fixtures file depends on. Confirm the `MODIFIED` block in `specs/toolkit-structure/spec.md` narrows that clause to `.md` files, that its `SHALL` still binds placeholders only, and that the added scenario states a fact about discovery without imposing an obligation — so the block adds no constraint and relaxes none. The companion-file obligation lives in `agent-authoring`, which is the capability that has one to govern; extending `toolkit-structure`'s `SHALL` to cover it would be a new constraint and would falsify this change's own non-goal
- [ ] 3.8 Edit `skills/create-skill/SKILL.md`'s **description** so its closing clause names `create-agent` as the agent-authoring standard instead of referring agent authoring to `AGENTS.md`, which will carry only a pointer. Do this before task 5.1, so the re-run evaluates the description that ships
- [ ] 3.9 Edit `create-skill`'s description standard so library-wide disambiguation is stated against every *asset* — every skill and every agent — rather than every skill, with the reason: the dispatch decision spans both types, so a description disambiguated only against skills can pass the standard and fail its own trigger check
- [ ] 3.10 Edit `create-skill`'s *Choosing tags* section so the vocabulary presented before tags are chosen spans `skills/` and `agents/`, matching `AGENTS.md`, which already scopes the synonym check to the repository rather than to one directory
- [ ] 3.11 State in `create-skill` that a description edit invalidates the recorded trigger check and requires a re-run, and that reporting an outcome in the authoring session and recording one in `SKILL.md` are different obligations — the first is required, the second is what task 3.6 forbids. Without the distinction stated, `skill-authoring` reads as requiring outcomes to be reported and forbidding them to be recorded, with nothing saying those are different acts
- [ ] 3.12 Note in `create-skill`'s description standard that the third-person rule governs skills, and that `agent-authoring` permits an agent's description to open `Use this agent when [conditions]` because every published source for agents prescribes it and it addresses the dispatcher. Both rules exclude the same failure — a description addressed to a human — so the divergence is in the permitted opener alone. Without the note, a reader of the skill standard applies third person to agents and contradicts the other capability
- [ ] 3.13 State in `create-skill` that the routing recorded for a fixture is the correct destination as confirmed by a passing run, not whatever a run produced. A run showing the prompt on the wrong asset is a description defect to fix and re-run; recording it would turn the fixture into a snapshot of current behaviour and cost it the ability to detect the same drift later

## 4. Verify `create-agent`

- [ ] 4.1 Run `create-skill`'s post-write validation against `create-agent` — frontmatter parses, `name` matches the directory, path is discoverable, tags are lowercase kebab-case, description states action and triggers, and the description states that the skill authors into this repository's library
- [ ] 4.2 Run the trigger check with a fresh-context evaluator holding every skill and agent description in the library — positive prompt must activate `create-agent`, negative must not
- [ ] 4.3 Choose the negative prompt so it could plausibly misfire onto `create-skill`, since those two are the most confusable descriptions in the library. Separately, confirm `create-agent`'s description states that it authors into *this repository's* `agents/` — that is the distinction that holds against `plugin-dev`'s `agent-development` skill and `agent-creator` agent, which the library-scoped evaluator cannot see and which trigger on the same phrasings
- [ ] 4.4 Revise the description and re-run on either failure; record both prompts and their expected routing in a `## Trigger check fixtures` section, without the outcome or the run date
- [ ] 4.5 Confirm no cold-run check is run against `create-agent`, and that the body says why — it is a skill, and loads into an existing context
- [ ] 4.6 Dry-run the full cold-run procedure once against `claude-security/agents/explore.md` — it carries an explicit dispatch contract, so the declared-property criteria are exercised rather than only the mechanics. **Run it isolated.** Its grant is `tools: Read, Glob, Grep, Bash`, and the isolation rule triggers on write, edit, or shell capability; the rule keys on the grant rather than on the body, which here confines Bash to read-only commands, precisely because a body's self-restriction is not enforced by the harness. Dry-running unisolated would make the change's one execution of its own procedure a non-conforming one and would report the procedure executable on the strength of a branch it never took. That plugin is present in the marketplace clone but not installed, so make the target dispatchable first with `--plugin-dir` against its plugin directory, run from the worktree; the dry run's value over task 1.6 is that it exercises the *preferred* branch, and one that silently falls back tests the fallback twice and the preferred form not at all. Record in `design.md` whether the procedure is executable as written, whether the agent's own report was recoverable or only the dispatcher's account of it, whether the pass criteria were decidable by a reader who did not write the body, whether the isolation mechanism composed, and — if the fallback was used anyway — that it was. Record also that this target cannot show the isolation *contains* a write, so that half still ships untested per `design.md` D11. No agent is added to `agents/`
- [ ] 4.7 Revise the cold-run procedure if 4.6 found it unexecutable as written or its pass criteria undecidable, in **both** surfaces — the `create-agent` body and the `agent-authoring` delta — since the delta is what task 6.4 syncs. A finding recorded but not applied ships a requirement this change already knows does not run

## 5. Re-run the invalidated check

- [ ] 5.1 Re-run `create-skill`'s recorded negative prompt — "write me a subagent that reviews my commit messages for clarity before I push" — against the library now containing `create-agent`, with task 3.8's description edit already in place so the evaluator reads the text that ships
- [ ] 5.2 Record which asset the prompt now reaches, rather than confirming an expected one, and confirm `create-skill`'s recorded positive prompt still activates `create-skill`. Three outcomes are live and each has a different resolution: it reaches `create-agent`, which is the routing D2 predicts; it reaches `create-skill`, which is the confusability failure 5.4 handles; or it still reaches nothing, which is a defensible result rather than a bug — `create-agent` authors only into this repository's `agents/`, while the prompt asks for a subagent for the user's own project, so an evaluator may correctly decline it
- [ ] 5.3 Reshape `create-skill`'s `## Recorded trigger check` into a `## Trigger check fixtures` section — keep both prompts, record the negative's expected routing, and drop the stored outcomes and the run date per task 3.6. The routing recorded is the *correct* destination as confirmed by a passing run, not simply what 5.2 produced: an observed misfire is a description defect for 5.4 to fix and re-run, and recording it would leave a fixture that can never detect the drift it was created for. If the routing is "nothing" and that is correct — the prompt asks for a subagent in the user's own project, which `create-agent` does not author — record that with the reason, or replace the negative with a library-scoped prompt and note the substitution
- [ ] 5.4 If the two skills prove confusable, narrow whichever description is over-reaching and re-run both skills' checks

## 6. Close out

- [ ] 6.0 Reconcile **all three deltas** — `specs/agent-authoring/spec.md`, `specs/skill-authoring/spec.md`, and `specs/toolkit-structure/spec.md` — against the empirical findings of tasks 1.6, 1.7, 1.9, 1.10, and 4.6 before anything is synced. The deltas were written ahead of those tasks and are hedged where the outcome was open, but hedging is not reconciling: if 1.9 found that an agent carrying `metadata` fails to load, `agent-authoring` still says tags MAY be declared; if 1.6 found the agent's own report unrecoverable, the preference for a real dispatch is dead text that should say so; if 1.10 found that a populated worktree still does not register the drafted agent, the isolation requirement prescribes a procedure that does not run; if 4.6 found the procedure unexecutable, 4.7's revision has to be reflected here too. The scope is all three because a finding does not respect delta boundaries: task 1.7 tests `.yaml` discovery, and `toolkit-structure` carries a scenario asserting that a non-`.md` file beside an agent is neither discovered nor a parse failure — a clause 1.7 can falsify in a file an `agent-authoring`-only reconciliation would never open. Correct any clause a finding falsified rather than carrying it into `openspec/specs/`
- [ ] 6.1 Confirm `agents/README.txt` is untouched and `agents/` is still empty, since this change adds no agent
- [ ] 6.2 Confirm no executable code was added to the repository
- [ ] 6.3 Run `openspec validate add-create-agent` and confirm the change is valid
- [ ] 6.4 Sync `agent-authoring` and the `skill-authoring` and `toolkit-structure` deltas into `openspec/specs/`
- [ ] 6.5 Strip the stray `</content></invoke>` tool-call residue from the end of `openspec/specs/toolkit-structure/spec.md`, left by the previous change's sync. It is pre-existing and sits after the last requirement, so the `toolkit-structure` delta does not displace it — but this change now writes into that file, and stripping it after the sync rather than before keeps the two edits from racing
- [ ] 6.6 Archive the change
