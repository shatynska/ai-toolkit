## 1. Intent Checkpoint

- [x] 1.1 Confirm the routing gate: on-demand procedural knowledge belonging in the caller's context, so a skill rather than a rule fragment, a command, or an agent. Record the outcome even though this was walked conversationally during exploration.

  Outcome: Confirmed. Several thousand words of on-demand procedural knowledge applied in the caller's working tree against the caller's test files — not a short standing instruction (rule), not a deterministic named sequence (command), and not work needing its own execution context (agent). A skill is the right artifact. The agent that would apply this standard inside the OpenSpec flow is deliberately a separate, deferred change, per proposal.md.
- [x] 1.2 Confirm the name `testing` is free by listing `skills/` in this repository.

  Outcome: Confirmed free. `skills/` contains `bash`, `create-agent`, `create-skill`, `langgraph`, `n8n-workflow-review`, `python`, `terraform` only.
- [x] 1.3 List the `metadata.tags` in use across `skills/` and `agents/` and confirm `testing` is genuinely new rather than a synonym of an existing label (`review` is the closest existing tag and the one most likely to be argued for instead — record why it does not fit, or adopt it).

  Outcome: Tags in use: `authoring`, `hitl` (`create-skill`, `create-agent`), `bash` (`bash`), `infrastructure`, `terraform`, `hitl` (`terraform`), `langgraph` (`langgraph`), `n8n`, `review` (`n8n-workflow-review`), `python` (`python`), `review`, `openspec` (`openspec-change-reviewer`). `testing` is genuinely new and not a synonym of any. `review` was considered as the task requires and rejected: it marks assets that *perform* a review of existing artifacts (`n8n-workflow-review`, `openspec-change-reviewer`), whereas reviewing tests is one of three entry surfaces this skill covers rather than its subject. Adopting `review` would classify the skill by one of its entries and collide with an established meaning. New tag `testing`, reason recorded here per `toolkit-structure`.
- [x] 1.4 Confirm the skill's purpose in one or two sentences, matching proposal.md's Why.

  Outcome: A language-agnostic testing floor: the failure modes that make an agent-authored test suite look rigorous while establishing nothing — an uninterpretable failure claim, a failure misread for what it proves, an assertion the author invented, and a test weakened to reach green — with stack specifics routed by pointer to the skill that carries their dated verification. Matches proposal.md's Why.
- [x] 1.5 Confirm the triggering conditions — the phrasings a user would plausibly type — against the description drafted in Checkpoint 2, including at least one stack-naming phrasing ("write tests for this function", "write a pytest for this"), per design.md Decision 2.

  Outcome: Triggering conditions: "write tests for this function", "write a pytest for this", "add tests for this module", "review my tests", "is this test any good", "why does this test pass when it shouldn't", "why is this test failing", "should I mock this or not". Covers all three surfaces the delta spec's triggering requirement mandates — write, review, debug — including at least one stack-naming phrasing. Checked against the drafted description at 2.1.
- [x] 1.6 Confirm the target is this repository's own `skills/`, not any project the skill is written for.

  Outcome: Confirmed — `skills/testing/` in this repository (`ai-toolkit`), not any project the skill is written for.
- [x] 1.7 Present the Intent outcomes for approval and pause, per `skill-authoring`'s human-in-the-loop checkpoint sequence, which requires a confirmation pause at Intent rather than agent self-confirmation. (Task 1.5's forward reference to Checkpoint 2 is deliberate and matches `add-langgraph-skill`'s archived `tasks.md`, where the same ordering was used and completed without difficulty: the triggering *conditions* are confirmed here, and the drafted description is checked against them in 2.1. Recorded so a later review does not re-raise it as an inversion.)

  Outcome: presented and approved by the user in the apply session. The routing gate, the free name, the new `testing` tag (with `review` considered and rejected on the recorded grounds), the purpose statement, the eight trigger phrasings across all three mandated surfaces, and the target directory were all confirmed before any drafting began.

## 2. Shape and Draft Checkpoints

- [x] 2.1 Draft the frontmatter — `name`, `description`, `metadata.tags: [testing]` — with no `allowed-tools`, per design.md Decision 7. Verify the description is third person, names adjacent trigger phrasings, states the language-agnostic boundary, states the co-trigger relationship with the language skills explicitly (per the spec's *Triggering Covers Test-Writing Requests and Accepts Overlap* requirement), and disambiguates against every asset in the library *and* the OpenSpec assets committed under `.claude/`.

  Outcome: Drafted. `name: testing`, `metadata.tags: [testing]`, no `allowed-tools` (open-ended guidance, not a wrapper around one command), description as presented at 2.6. Third person opener; names four trigger phrasings covering all three mandated surfaces; states the language-agnostic boundary and the no-framework-specifics rule; states the co-trigger relationship explicitly ("this skill supplies the standard, those skills supply the idiom and carry its version-dated verification"); disambiguates against `create-skill`/`create-agent` (library authoring), the OpenSpec assets committed under `.claude/` (proposals and reviews), and `n8n-workflow-review`.
- [x] 2.2 Before drafting, record the precedence order in which 2.1's obligations yield if the 1024-character budget binds, so the cut is a decision rather than whichever clause is easiest to shed. The explicit co-trigger statement is mandated by the `testing-practice` spec and yields **last**; trigger phrasings beyond the first two yield **first**. Then verify the drafted description is at most 1024 characters and contains no angle brackets, counting **by script rather than by estimate** — the estimate recorded at draft time in `add-langgraph-skill` was wrong by ~30% and was caught only at post-write validation.

  Outcome: Precedence recorded before drafting, per the task: the co-trigger statement is spec-mandated and yields last; trigger phrasings beyond the minimum yield first. Bounded by the delta spec's review/debug triggering requirement, the minimum is one phrasing per mandated surface, so phrasings cannot be cut below three. First draft measured 1037 characters by script — over the 1024 limit, vindicating the task's insistence on a scripted count rather than an estimate. Applied the precedence: dropped "add tests for this module" (a second write phrasing) and "why is this test failing" (a second debug phrasing), keeping four across three surfaces. Re-measured: 980 characters, no angle brackets. The co-trigger clause was never at risk, which is what the precedence order was written to guarantee.
- [x] 2.3 Draft the body outline as section headers only. Confirm every requirement in the `testing-practice` delta spec traces to at least one section: language-agnostic scope and the no-dated-claims boundary; the two entry situations; baseline (including the unobtainable case); the four failure states; the target-absent prohibition; level selection; specified/derived/untested and its recording surface; never weakening an existing test; how the floor applies on a review or debug entry; the pointer section; bounded deference to project conventions.

  Outcome: Outline drafted as eleven section headers; all twelve delta requirements trace. Sections: When this applies (req 1) - Which situation you are in (req 2) - Take a baseline first (req 3) - What a failing test establishes (req 4, including the provenance rule) - An absent target is not a problem to fix (req 5) - Choosing the level (req 6) - Specified, derived, deliberately untested (req 7) - Never weaken a test to reach green (req 8) - Reviewing or debugging rather than writing (req 11) - Read the project's conventions first (req 12) - Language and framework specifics (req 9). Requirement 10 (triggering) is satisfied by the frontmatter description rather than a body section, which is the correct home for it; recorded here so the trace is not read as incomplete.
- [x] 2.4 Apply design.md Decision 5's two scope filters to the drafted outline explicitly — does each section change what an agent does, and can it be stated without naming a language? Cut anything failing either, and record what was cut. Exempt the scope and provenance statements required by 3.1 (the language-agnostic boundary and the absent dated-verification line): they orient the reader to what the skill does and does not assert, which the filters are not aimed at, and without this exemption 2.4, 3.1 and 3.11 give opposing instructions on the same paragraph. Keep the two-of-four precedent detail in the change record rather than the body. This is the first practice-shaped skill in the library, so the bar is being set here for later ones to cite.

  Outcome: Applied both filters. Every section survives the behavior filter: each states something an agent would otherwise get wrong (baseline omitted, a state misread, an assertion invented, a test weakened) rather than something a competent model already supplies. Every section survives the language filter: no section requires naming a runner to state, and the pointer section exists precisely to export what does. Cut during outlining: a planned "test naming and structure" section (fails the behavior filter - conventional advice a model already supplies) and a planned "what to do about flaky tests" section (fails the language filter in practice, and is test-suite architecture, which Non-Goals exclude). Per the exemption added to this task, the scope and provenance statements required by 3.1 are exempt from the behavior filter: they orient the reader to what the skill does and does not assert, which the filters are not aimed at.
- [x] 2.5 Re-check design.md Decision 4's flat-structure projection against the drafted outline's estimated length. If the body will not read in one pass, split into `references/` and record the divergence from the decision rather than silently exceeding the budget.

  Outcome: Re-checked. Eleven sections, projected at roughly 2,800-3,500 words - within `create-skill`'s single-pass budget of about five thousand. Decision 4's flat projection holds, and the reason it holds is Decision 3: the pointer section exports every language-specific expansion to a sibling skill, which is the growth that would otherwise force a split. The two requirements added during review (the two-situations requirement and the review/debug entry) add roughly 300 words between them and do not change the conclusion. This remains a projection; it is re-verified against the written body at 4.6, and a split is taken then rather than pre-emptively if the estimate proves wrong.
- [x] 2.6 Present frontmatter and outline for approval. Write no file before this is approved; apply revisions to the draft and re-present rather than writing and editing.

  Outcome: frontmatter and outline presented in the apply session and approved by the user. No file was written before approval. Presented items: the 980-character description (with the over-budget first draft and the applied yield order reported), `metadata.tags: [testing]`, no `allowed-tools`, the eleven-section outline with its requirement trace including the note that requirement 10 is carried by the description rather than a body section, the two sections cut at 2.4, and the flat-structure projection flagged as re-verified at 4.6.

## 3. Write the Skill

- [x] 3.1 Create `skills/testing/SKILL.md` with the scope statement: language-agnostic, holds for any runner or project shape, and carries no version-dated claims — including a brief statement that this skill deliberately carries no "verified live against version X" line and why, per design.md Decision 6, so its absence is not read as an oversight. Keep this to the boundary itself; the supporting detail (that only `python-practice` and `langgraph-practice` mandate such a statement, so the divergence is from two of four precedents rather than a uniform one) belongs in the change record, not the body — see 2.4's exemption.

  Outcome: Written. `skills/testing/SKILL.md` opens with the floor framing (like `bash`/`python`/`terraform`, holds regardless of language, runner or purpose; not a tutorial, not a coverage policy) and states the no-version-dated-claims boundary plus the deliberate absence of a 'verified live against version X' line, with the reason. Per 2.4's exemption the body carries only the boundary itself; the two-of-four precedent detail is in this change record, not in the skill.
- [x] 3.2 Write the **two entry situations** section, per design.md Decision 0: the target does not exist yet, or it already does; which rules bind in each; and that a test passing on first run is an alarm in the first and the expected result in the second. State explicitly that this is not an argument for writing tests first, so the section is not later read as contradicting the "not TDD advocacy" Non-Goal. Place it ahead of the four-states section (3.4), which depends on it.

  Outcome: Written as *Which situation you are in*. Both situations named, the pass-on-first-run inversion stated explicitly in both directions, the absent-target rule marked as binding only in the first situation and the baseline/level/classification/never-weaken rules marked as binding in both. Closes with the explicit disclaimer that none of it argues for writing tests first. Placed ahead of the four-states section, which depends on it.
- [x] 3.3 Write the baseline section: run the existing suite and record what already failed before any new test is written, and state why a claim of failure is otherwise uninterpretable. Cover the case where **no baseline can be taken** — no suite, no runner, or a suite that cannot execute here — including the executable-but-impractical case (prohibitively slow, or needing credentials or services unavailable here). Present the **scoped** baseline — covering the tests bearing on the target, recorded with its scope — as a first-class permitted form rather than a fallback, since attributability is the requirement's purpose and a full run as default imposes a cost whose predictable adaptation is silent omission. Where not even a scoped baseline is possible, an explicit statement that none was taken and why is carried alongside any later failure claim. Neither silent satisfaction nor a reported-but-unrun baseline is permitted. State where a baseline is recorded.

  Outcome: Written as *Take a baseline first*. Scoped baseline presented as a first-class option rather than a fallback, with the attributability reasoning and the silent-omission failure it prevents. Covers all four unobtainable cases including executable-but-impractical. Requires stating that no baseline was taken and why, and names the recording surface.
- [x] 3.4 Write the four-states section. Define each state by what it establishes — code ran and produced a wrong value; target absent so assertions are unverified; the test itself is broken so it establishes nothing; it passed on first run. **Name no state after a runner's phase, an error class, or an exit status**, per design.md Decision 1. State the enumeration for the absent-target situation and cross-reference 3.2 for how the second and fourth states read when the target already exists, rather than restating the list. State the third state as **situation-independent**: a defect in the test establishes nothing about the code either way, and repairing it moves the test to another state rather than producing evidence. Do not write that a repaired test yields a meaningless pass — that belongs to the fourth state. Write the **provenance rule** that separates the first state from the third, per design.md Decision 1: a specified assertion that mismatches means the code is wrong and the test is not a candidate for repair; only a derived assertion may be reconsidered, and then as a recorded change rather than a repair; a defect not in the expected value at all (wrong import, malformed setup) is the third state regardless. Without it the third state is a re-description available for any failure, and the never-weaken rule becomes satisfiable by relabelling. Route the remaining diagnostic question ("which state is this failure in?") to the language skills.

  Outcome: Written as *What a failing test establishes*, four states each defined by what it establishes, none named for a phase, error class or exit status. Enumeration scoped to the absent-target situation with a cross-reference to 3.2 for the second and fourth, and the third stated as situation-independent. Carries the provenance rule under its own subheading *Telling the first state from the third*, with all three branches (specified means the code is wrong; only derived may be reconsidered and then as a recorded change; a defect outside the expected value is state 3 regardless) and the explicit statement of what it forecloses. Remaining diagnosis routed to the language skills.
- [x] 3.5 Write the target-absent prohibition: an absent target is an acceptable outcome, and creating the module, function, type, or empty stub to make tests execute is writing implementation. State that this is the most likely drift point, because it presents itself as repairing the tests. State both its scope and its lifting condition, per design.md Decision 8: it binds while the task's scope is writing tests, and where the same task also authorises implementation it lifts once the target-absent failure has been reported, after which the implementation is written as a distinct, announced step. What stays prohibited throughout is creating a target *in order to make tests execute*. An unbounded version would forbid ordinary red-green work and get overridden wholesale.

  Outcome: Written as *An absent target is not a problem to fix*. States the prohibition, names it as the most likely drift point with the reason, and gives the scope and lifting condition as a two-item list: binds while the task's scope is writing tests, lifts once the failure is reported where the same task authorises implementation, after which implementation is a distinct announced step. States that creating a target in order to make tests execute stays prohibited throughout.
- [x] 3.6 Write the level-selection rule: the level is the smallest unit that can observe the expected outcome, and a level above that minimum buys no additional evidence while costing speed and determinism.

  Outcome: Written as *Choosing the level*. States the rule, gives the reading procedure (what must exist for the outcome to be observable), and states that a level above the minimum buys no evidence while costing speed and determinism. Notes it resolves the question without appeal to preference or a target ratio.
- [x] 3.7 Write the specified/derived/deliberately-untested section, including why an unlabelled derived assertion obliges an implementer to satisfy a constraint nobody agreed to, and why an uncovered case is recorded with its reason rather than omitted. State the recording surface — completion report, annotation alongside the tests, or either at the project's discretion — rather than requiring recording without saying where.

  Outcome: Written as *Specified, derived, deliberately untested*. Three categories defined, the obliges-a-constraint-nobody-agreed-to reasoning stated, the link to the provenance rule made explicit, the uncovered-case-with-reason rule stated, and the recording surface named.
- [x] 3.8 Write the never-weaken section: no editing an assertion to match observed output, no deleting or disabling a failing test to reach green, and a test superseded by a changed requirement is reported as superseded rather than quietly rewritten. State explicitly that describing such an edit as repairing a broken test does not exempt it, and cross-reference 3.4's provenance rule, which forecloses that route for a specified assertion.

  Outcome: Written as *Never weaken a test to reach green*. Both prohibitions stated, the records-instead-of-constrains consequence given, the superseded-requirement path stated, and — cross-referencing 3.4 — the explicit statement that describing such an edit as repairing a broken test does not exempt it, because the provenance rule forecloses that route for a specified assertion.
- [x] 3.9 Write the **bounded deference** section, per design.md Decision 8: a consuming project's `AGENTS.md`, `CLAUDE.md` and existing tests override this floor on level vocabulary, recording surface and stubbing approach; the never-weaken and target-absent prohibitions do not yield; a conflict is reported rather than silently resolved; and an absent convention produces a question rather than an invention. State which two rules are non-negotiable and why, so the boundary is reviewable rather than a matter of tone.

  Outcome: Written as *Read the project's conventions first*. Deference declared with the 'except as bounded below' qualifier so the paragraph does not contradict the carve-out; read-first and report-conflict instructions given; absent-convention case produces a question. The two non-negotiable rules are listed with the reason a floor deferring on them asserts nothing, and with the explicit note that this is not licence to act unannounced — the conflict is still reported. Ordinary deference enumerated.
- [x] 3.10 Write the pointer section naming every domain skill covering a stack with a testable artifact — `python`, `langgraph`, `bash`, **and `terraform`** — as where stack-specific testing material lives, directing that the matching skill be loaded **before** tests are written, and stating the reason (those skills carry the dated verification their claims require). Do not restate their content. Do not omit a skill for currently having thin testing content: that is a fact about its present state, not its scope, and omission would leave a request in that stack with no route onward. Note in the change record that these targets are currently thin — proposal.md scopes filling them as follow-on changes. Record a determination (do not assume, as task 6.2 requires for its own sweep) for every remaining library asset, naming why `n8n-workflow-review` and any other does or does not cover a stack with a testable artifact, so a later reader can tell the exclusion was reasoned rather than overlooked.

  Outcome: Written as *Language and framework specifics*. Names `python`, `langgraph`, `bash` and `terraform` with what each carries, directs that the matching skill be loaded before writing tests, and states the version-dated-verification reason. Closes with the explicit rule that a skill currently carrying little on testing is still named, since that is a fact about its present state and not its scope. Determination for the remaining library assets: `n8n-workflow-review` is not named — it is a review checklist applied to a workflow JSON export, not a domain skill for a stack whose artifacts are exercised by a test suite, so it has no testing material to own; `create-skill` and `create-agent` are authoring standards for this library rather than stacks; `openspec-change-reviewer` is an agent, not a skill. Four named, one excluded with reason, three not applicable.
- [x] 3.11 Write the **review and debug entry** section, per design.md Decision 2: the specified/derived classification is the review question, the four failure states are the debug vocabulary (a test passing when it should not is the fourth state), and rules that presuppose an authoring pass — baseline before writing, level for a test not yet written — are marked as such. Task 5.4 gates completion on the skill firing for these phrasings, so without this section it fires and supplies content for a task the user is not performing.

  Outcome: Written as *Reviewing or debugging rather than writing*. Maps the review entry to the specified/derived classification (naming the unlabelled derived assertion as the common real finding) and the debug entry to the four states (fourth state for a test passing when it should not, third state plus the provenance rule for a test failing for the wrong reason). Explicitly names what does not apply on these entries — baseline before writing, and level selection for a test that already exists — rather than leaving the reader to infer it.
- [x] 3.12 Review the whole body against the Non-Goals: no coverage policy, no framework API or fixture idiom, no test-suite architecture or CI shape, no advocacy for TDD as a methodology, no testing-genre catalogue. Remove anything a competent model already supplies unprompted so it does not displace the trap-focused content. Confirm specifically that 3.2's two-situations section reads as a statement of what each situation establishes, not as a recommendation to adopt one.

  Outcome: Reviewed by targeted grep plus a full read. No coverage policy (the only 'ratio' hit is the sentence explicitly *rejecting* a target ratio between test kinds; the 'percent' and remaining hits are 'illustration' and 'situation'). No framework API or fixture idiom in the body — no `pytest.`, `@fixture`, `conftest`, `describe(`/`it(`. No CI, sharding, parallelisation, quarantine or directory layout. No TDD advocacy: the one place the question arises, *Which situation you are in*, closes by disclaiming it. No testing-genre catalogue. Confirmed specifically per this task that 3.2's section reads as a statement of what each situation establishes rather than a recommendation to adopt one.
- [x] 3.13 Check every example in the body: each must be marked illustrative rather than presented as verified behavior of a named tool, per the spec's *Guidance Is Language-Agnostic* requirement. Confirm separately that no **category** is language-shaped — the labelling rule does not substitute for design.md Decision 1's structural rule.

  Outcome: Checked both rules separately. Labelling: the only failure-message reference in the body is the blanket statement at the top that any message or command shown is an illustration of a distinction, not a claim about a named tool — and in the event no per-tool failure output was needed anywhere, so nothing else required marking. Structural: the four states are named 'The code ran and produced a wrong value', 'The target does not exist yet', 'The test itself is broken', and 'It passed on its first run, before any implementation existed' — every one named by what it establishes, none by a phase, error class or exit status. Grep confirms no occurrence of collection phase, ModuleNotFoundError, ImportError, compile error, command-not-found or exit code anywhere in the file. Tool names (`pytest`, ShellCheck, `terraform test`) appear only inside the pointer section, which is where Decision 3 puts them.

## 4. Post-Write Validation

- [x] 4.1 Confirm the frontmatter parses as valid YAML.

  Outcome: Parses cleanly (`yaml.safe_load`). Top-level keys are `name`, `description`, `metadata` only — all schema-defined per `create-skill`'s verified field list, and no `version` or other non-schema key at top level.
- [x] 4.2 Confirm `name` matches the directory name (`testing`), and that the file sits at `skills/testing/SKILL.md` with no directory between `skills/` and the skill's own directory.

  Outcome: `name: testing` matches the directory `skills/testing/`. File sits at `skills/testing/SKILL.md` with no directory between `skills/` and the skill's own directory (path depth verified by script).
- [x] 4.3 Confirm declared tags are lowercase kebab-case.

  Outcome: One tag, `testing`, matching `[a-z0-9]+(-[a-z0-9]+)*` — lowercase kebab-case.
- [x] 4.4 Confirm the description states both the action and the triggering conditions.

  Outcome: The description states the action (writing, reviewing, debugging tests) and the triggering conditions (four quoted phrasings across all three surfaces), plus the language-agnostic boundary, the co-trigger relationship, and disambiguation against library and `.claude/` assets. 980 characters, no angle brackets, no `allowed-tools`.
- [x] 4.5 Confirm every bundled resource the body references actually exists — including that each skill named in the pointer section (task 3.10) is a real directory under `skills/`.

  Outcome: All four skills named in the pointer section exist as real directories: `skills/python/SKILL.md`, `skills/langgraph/SKILL.md`, `skills/bash/SKILL.md`, `skills/terraform/SKILL.md`. No `references/`, `scripts/` or `assets/` are referenced by the body, so there is nothing else to check.
- [x] 4.6 Re-read the body against each requirement of the `testing-practice` delta spec — all eleven — and confirm every one is satisfied by written content, not merely by an outline heading.

  Outcome: Re-read against all twelve `testing-practice` requirements; every one is satisfied by written content rather than a heading. 1 language-agnostic/no-dated-claims -> opening section, both scenarios (same answer across stacks; no dated statement, stated explicitly with its reason). 2 two situations -> *Which situation you are in*, both scenarios incl. the pass-inversion. 3 baseline -> *Take a baseline first*, all four scenarios incl. scoped-as-first-class, the four unobtainable cases, and the recording surface. 4 four states -> *What a failing test establishes*, all four scenarios, with the provenance rule under its own subheading. 5 target-absent -> *An absent target is not a problem to fix*, both scenarios incl. the implementation-authorised path. 6 level -> *Choosing the level*. 7 specified/derived/untested -> its own section, both scenarios plus the recording surface. 8 never-weaken -> its own section, both scenarios, incl. the relabelling foreclosure. 9 pointer -> *Language and framework specifics*, both scenarios incl. the no-omission-for-thin-content rule. 10 triggering -> carried by the frontmatter description, verified in section 5 rather than by a body section, as recorded at 2.3. 11 review/debug entry -> *Reviewing or debugging rather than writing*, both scenarios, incl. the explicit statement of which rules do not apply. 12 bounded deference -> *Read the project's conventions first*, all three scenarios, with the `except as bounded below` qualifier so paragraph one does not contradict the carve-out. Body measures 2,301 words against `create-skill`'s roughly five-thousand-word single-pass budget, so Decision 4's flat structure is confirmed against the written body and no `references/` split is taken — the 2.5 projection of 2,800-3,500 was conservative.

## 5. Trigger Check Under the Amended Standard

- [x] 5.1 Build the fresh-context evaluator per `skill-authoring`: the candidate prompt plus the `name` and `description` of every asset in `skills/*/SKILL.md` and `agents/*.md`, **and** every skill and command committed under `.claude/` (`.claude/skills/*/SKILL.md`, `.claude/commands/**/*.md`). Derive both sets by glob, not from a fixed list. Nothing from the authoring conversation.

  Outcome: Built by glob as required, not from a fixed list: `skills/*/SKILL.md` (8, now including `testing`), `agents/*.md` (1), `.claude/skills/*/SKILL.md` (6), `.claude/commands/**/*.md` (6) — 21 assets, name and description only. Run through `claude -p` in a fresh process rather than in the authoring context, which was essential here: the `testing` skill registered in the authoring session the moment the file was written, so an in-context check would have fired regardless of description quality — the precise non-evidence `skill-authoring` warns against. The evaluator was instructed to consider only the supplied catalogue and ignore its own environment, keeping scope to this repository rather than to whatever plugins are installed on this machine.
- [x] 5.2 Record the amended pass standard before running, per design.md Decision 6: because overlap with the language skills is intended, a prompt routing to **both** `testing` and a language skill is a **pass**. Only displacement counts as a failure — `testing` routed to where a language skill should have been reached, or a language skill reached where `testing` should also have fired.

  Outcome: Amended standard recorded before running: a prompt routing to both `testing` and a language skill is a PASS, because Decision 2 makes overlap intended. Only displacement fails — `testing` reached where a language skill should have been, or a language skill reached where `testing` should also have fired.
- [x] 5.3 Run the first positive prompt: a stack-naming test-writing request ("write tests for this function" against a named language). `testing` must fire. A language skill firing alongside it is a pass, not a failure.

  Outcome: PASS. Prompt: "Write tests for this function in my Python module." Routed to `testing, python`, with the justification that testing supplies the language-agnostic standard and python the runner idiom. `testing` fired, and the co-trigger is a pass under 5.2 rather than a collision — this is the intended relationship working exactly as Decision 2 describes it.
- [x] 5.4 Run a second positive prompt on a **review or debug** phrasing ("review my tests for this module", "why does this test pass when it shouldn't"). The `testing-practice` spec's triggering requirement names writing, reviewing, and debugging, so a description could satisfy 5.3 while never firing on two of the three phrasings it mandates. `testing` must fire.

  Outcome: PASS. Prompt: "Why does this test pass when it shouldn't?" Routed to `testing` alone. Confirms the review/debug surface fires, which the delta spec mandates and which 5.3 alone could not establish — the evaluator explicitly noted the phrasing is named in the description and that no language was specified to pull in a companion skill.
- [x] 5.5 Run the negative prompt: an adjacent, plausibly-misfiring prompt outside scope — **splitting a slow suite across CI runners** (test-suite architecture and CI shape, both Non-Goals). `testing` must not fire. Do not use an unrelated prompt, which proves nothing. Do **not** use a coverage-target prompt here: choosing a coverage target is a testing question that a description written to counter under-triggering will plausibly reach, so failing on it would drive a narrowing loop against the breadth 5.3 and 5.4 require, with no tie-breaker. CI sharding is outside the subject entirely, which is what a negative prompt needs. Determine and record **which asset the evaluator holds should serve it** — or that none should, with the reasoning — per `skill-authoring`'s requirement that a negative fixture name the asset that should serve the prompt where one exists. A fixture asserting only "reaches nothing" is falsified silently by the next asset added.

  Outcome: PASS. Prompt: "Our test suite takes 40 minutes in CI. How should we split it across runners?" Routed to NONE; `testing` did not fire. The evaluator's reasoning matched the Non-Goals exactly — CI infrastructure and parallelisation rather than test correctness discipline. Expected-routing determination required by this task: NONE is correct, and no asset the evaluator holds should serve it. `bash` and `terraform` cover authoring in their own stacks rather than CI pipeline shape; `terraform`'s description explicitly excludes building a CI pipeline around it; no library or `.claude/` asset covers CI configuration. Recorded as a determination rather than a bare assertion so a later addition that does cover CI falsifies this fixture visibly.
- [x] 5.6 Run a displacement probe in the reverse direction: a prompt that should reach a language skill on non-testing grounds ("review my Python script for bugs"). Confirm `testing` does **not** displace `python`. This is the risk design.md records as the intended-overlap failure mode and it is not covered by 5.3, 5.4 or 5.5.

  Outcome: PASS. Prompt: "Review my Python script for bugs." Routed to `python` alone; `testing` did not displace it. This is the intended-overlap failure mode Decision 2 records as its main risk, and it did not occur — the evaluator noted no tests were involved.
- [x] 5.7 Report all four outcomes. A positive-prompt failure (5.3 or 5.4) means the description is too narrow — widen and re-run. A negative or displacement failure (5.5 or 5.6) means it is too broad — narrow and re-run. The skill is not complete until all four hold. If widening and narrowing conflict across re-runs, stop and report rather than choosing between two artifacts' requirements unilaterally. Where a displacement traces to the **language skill's** description rather than to `testing`'s, also stop and report: editing another asset's description is outside this change's stated Impact, which admits no existing-asset edit beyond a falsified fixture, and needs a separate decision rather than a silent pass.

  Outcome: All four reported above: 5.3 PASS, 5.4 PASS, 5.5 PASS, 5.6 PASS. No widening or narrowing was needed, so neither the widen/narrow conflict clause nor the other-asset-description clause was reached. The description stands as written at 2.1 with no post-check edit, which means the 4.4 validation and these fixtures describe the same description.

## 6. Invalidation Sweep of Competing Assets

- [x] 6.1 Re-run the recorded trigger-check fixtures of `python`, `langgraph`, and `bash` against an evaluator that now includes `testing`, per proposal.md's Impact section. Evaluate under the amended standard from 5.2 — co-triggering passes, displacement fails.

  Outcome: All six fixtures re-run against the widened evaluator (21 assets, now including `testing`), under 5.2's amended standard. `python` positive (default-argument review) -> `python`, unchanged. `python` negative (build a LangGraph agent) -> `langgraph, python`, where the recorded fixture says `langgraph`. `langgraph` positive (recursion limit on handoff) -> `langgraph`, unchanged. `langgraph` negative (JS/TS SDK) -> NONE, unchanged — notable because a recorded 'nothing' claim is exactly what a library addition can silently falsify, and adding `testing` did not. `bash` positive (pipeline exit status) -> `bash`, unchanged. `bash` negative (Terraform replacement) -> `terraform`, unchanged. **`testing` fired in none of the six**, so no fixture was falsified by this change. On the one deviation: the `python` negative's extra `python` co-trigger is a `python`/`langgraph` interaction that `python`'s own description invites ('this skill's traps still apply to LangGraph code'), it predates this change, and `langgraph` was not displaced — a pass under 5.2. It is recorded here rather than acted on, per 6.3, which scopes edits to fixtures this change falsified.
- [x] 6.2 Determine (do not assume) whether `terraform`, `n8n-workflow-review`, `create-skill`, `create-agent`, and `openspec-change-reviewer` have fixtures at risk. Record the determination and its reasoning either way.

  Outcome: Determination for the remaining assets, made rather than assumed. `terraform`: has fixtures; not re-run. Its recorded pair concerns plan/replacement semantics and a shell-vs-provisioning boundary, neither of which a testing prompt reaches; `terraform test` is named in `testing`'s pointer section as material belonging to `terraform`, which routes toward that skill rather than away from it, so no competition is created. `n8n-workflow-review`: has fixtures; not re-run. It is a review checklist over a workflow JSON export, not a stack whose artifacts are exercised by a suite — the same determination recorded at 3.10 for its exclusion from the pointer section. `create-skill` and `create-agent`: have fixtures; not re-run. Both trigger on authoring library assets, which shares no phrasing with writing, reviewing or debugging tests. `openspec-change-reviewer`: has `.checks.yaml` fixtures; not re-run. It reviews an OpenSpec change's planning artifacts, and `testing`'s description explicitly disclaims OpenSpec proposals and reviews. All five judged not at risk, with the reasoning recorded so the determination is auditable rather than a bare assertion.
- [x] 6.3 Where a fixture is falsified, update it **in this change** rather than deferring, per proposal.md's Impact section.

  Outcome: No fixture was falsified by this change, so no fixture was edited. The single deviation observed at 6.1 is a pre-existing `python`/`langgraph` co-trigger that passes under the amended standard and is outside this change's stated Impact, which admits an existing-asset edit only for a fixture this change falsified. Recorded rather than acted on.

## 7. Record Fixtures

- [x] 7.1 Record this skill's trigger-check fixtures — both positive prompts (test-writing and review/debug), the negative prompt, the displacement probe, their expected routing, the amended pass standard, and why each prompt was chosen — in the location and form `skill-authoring` requires. Record why the coverage-target prompt was rejected as a negative, so a later session does not reintroduce it.

  Outcome: Recorded in a `## Trigger check fixtures` section in `skills/testing/SKILL.md`: both positives (authoring, and review/debug recorded separately with the reason an authoring prompt alone cannot establish the other two surfaces), the negative, and the displacement probe — each with its expected routing and why that prompt was chosen. The negative's expected routing is recorded as a determination naming why no asset should serve it (`terraform` explicitly disclaims CI pipeline shape), per `skill-authoring`'s requirement that a negative name its destination where one exists, so a later asset covering CI falsifies it visibly. The rejected coverage-target prompt is recorded with the narrowing-loop reasoning so it is not reintroduced. No outcome and no run date are recorded, per the same requirement — outcomes were reported in this session at 5.3-5.7 instead.
- [x] 7.2 Record the amended pass standard as part of the fixture itself, so a later run evaluates against it and does not read a co-trigger as a regression. Record the standard, not that a run occurred — `skill-authoring` records fixtures and not outcomes, and phrasing this as a run record would persist exactly what that requirement excludes.

  Outcome: The amended pass standard is recorded as part of the fixture section itself, stated before the fixtures so a later run applies it: co-triggering with a language skill is a pass, only displacement fails, and the section says explicitly that the first fixture looks like a regression if read against the ordinary exclusive-routing standard. Phrased as a property of the fixtures rather than as a record that a run occurred — `skill-authoring` records fixtures and not outcomes, and a run record would persist exactly what that requirement excludes.
