## 1. Amend `skills/testing/SKILL.md` — the situations

- [x] 1.0 Amend the section's opening sentence — *"Two situations, and one central rule reads oppositely in them."* Both halves are wrong at three: the count, and *oppositely*, which names a two-way contrast the third reading falsifies. This is the same defect class as the two phrases task 2.3 amends, in the sentence those fixes did not reach.
- [x] 1.1 Leave the heading *Which situation you are in* — it names no count — and add a third bullet beneath the two already there. Define it by what the assertion does, not by what the target is: *the assertion does not execute the behaviour it asserts*. Among the examples name a program's source read without being run, and a module imported only so a constant can be read out of it; `design.md` Decision 1 records why a kind-based line excludes cases the evidence contains, and why loading the target is not the same as exercising it. Do not write that the target cannot be absent — task 1.12 states what happens when it is.
- [x] 1.2 Extend the sentence that reads *"A test that passes on its first run means opposite things in the two"* to cover three, and say what the third means: nothing at all, because a check that read nothing would pass identically. Keep the existing two readings verbatim; they are correct and this change does not re-litigate them.
- [x] 1.3 State the obligation the third situation carries, conditioned on the check's steady state rather than on when it was written: a check that is green passes without discriminating, so the check is run over material the test supplies. Name the resulting test a *fixture-driven discriminator* here, where the obligation is stated, because the classification section later in the same document uses the term and a term defined away from its obligation drifts from it. Do not present it as optional rigour and do not name a fixture mechanism: a temporary directory, a string, an in-memory map are all the project's choice.
- [x] 1.4 State what a discriminator that comes back **negative** establishes — the check asserts nothing, which is the fourth state's second branch reached deliberately — and that such a check is repaired or reported, never left standing as coverage. Where it is reported rather than repaired, state the surface too: the finding is recorded wherever the project records the classification and its reasons — the surface tasks 1.9 and 1.11 name — because a check known to assert nothing, reported only in conversation, is this mechanism's own finding lost at the last step. Put all of it in the third situation's own text: task 2.2 tells a third-situation author the fourth state's alarm does not discriminate for them, which closes the route an inference would otherwise take. This is the one finding the whole mechanism exists to produce, and the first six drafts of this change installed the routing without ever saying where it ends.
- [x] 1.5 State what the obligation assumes about the check: that what it reads can be pointed at material the test supplies. Say the precondition out loud — this bears only on a check already in scope, being authored or modified — because the clause read alone fires on any pre-existing check and the cost of that misreading is the retrofit task 1.7 exists to prevent. Where such a check's target is fixed and redirectable only by rewriting how it reads, redirecting it is part of modifying it — not a discriminator that could not be run, which is a different fact with a different remedy. Do not illustrate this from this repository's own `tests/cases/`: they read `$TOOLKIT_ROOT`, which `tests/run.sh` exports, so pointing one at a fixture tree needs a different root and no rewrite at all.
- [x] 1.6 State the separator between the second situation and the third as **one** test, not two: whether the assertion executes *the behaviour it asserts* — that exact wording, not the coarser *executes its target*, which `design.md` Decision 1 records as misclassifying the import-for-constants case. Say that loading the target is not exercising the asserted behaviour, and name that case. Say explicitly that committedness is not the test in either direction — code that already exists is committed and stays in the second situation, and that same code's source read statically sits in the third. Do not reintroduce "can behave" as the criterion anywhere in the section; it is the wording this change corrects.
- [x] 1.7 Scope the obligation to a check **as authored or modified**; say what modifying one means — a change to what the check asserts or to how it reads its target, never an edit to the file containing it, so a rename or an edit to a neighbouring check drags nothing into scope; and say what it means for a check predating the rule: undischarged, not in breach, and no suite is retrofitted. Without the scope the requirement makes every already-green static check in every consuming project non-conformant the day it lands — raising test volume in the name of bounding it, which is the opposite of this change's second purpose.
- [x] 1.8 Write the shared-machinery clause into the same paragraph, in **both** directions, and decide the extraction case the two directions leave between them: editing machinery several checks delegate their reading to brings a check into scope only where that check's own assertion or its own result changes — so a shared edit does not sweep in every delegating check, and extracting a read into a helper does not take one out of scope. Where an extraction changes neither the assertion nor the result, the shared-machinery sentence governs and the check stays out of scope; say so, because the general sentence ("how it reads its target") and the shared-machinery sentence otherwise give opposite answers on exactly the refactor the deferred shared-arrangement change is about.
- [x] 1.9 Give the unrunnable case an outcome **and a surface**. The form is the baseline rule's — "state that none was taken, and why" — and do not invent a second one. The surface is not the baseline's: record it wherever the project records the classification and its reasons, and say so, because this requirement elsewhere objects to an obligation to record with no stated surface, and because a reader sweeping for outstanding discriminator work does not look in the baseline. The deferred manifest change would give it a slot of its own; until then this is where it lives.
- [x] 1.10 State who discharges the obligation and when. A fixture-driven discriminator supplies its own material, so it does not depend on the target's state and is written beside a still-red check, in the same pass, by the same author. Do not write or imply that the fixture waits for the check to go green: nothing re-dispatches a test author at that moment, so an obligation deferred there is owed by nobody.
- [x] 1.11 State what an observed failure establishes, who records it and when, and what it leaves owed. The actor is the **author**, the moment is inside the authoring pass before it ends, and the surface is the one task 1.9 names — wherever the project records the classification and its reasons, **not** the baseline record. The delta gives two reasons for keeping the unrunnable record out of the baseline and both hold of this one: the baseline is about the suite before these tests were written, and this is a fact about one test in this pass. A rule that files two materially identical pass-scoped records on different surfaces is unfollowable, which is why they share a surface. Do not oblige a second record once the check goes green — the ordinary suite result is the confirmation, and an obligation falling due after the pass has ended is owed by nobody, which task 1.10 states as a defect. A check red at authoring has discriminated on real content when it goes green, and the later green is read against the record named above — not against the baseline. Then state plainly that the fixture is **still owed once the check is green**, and give the reason rather than asserting it: the red run leaves nothing in the suite, so nothing re-establishes the property afterwards. Say explicitly that where tests are derived before implementation every check is red at authoring, so an exemption here would exempt every check in such a project. Do not write that every check over a static target passes on its first run — it is false in exactly this case, and it is the claim an earlier draft of this change made.
- [x] 1.12 Arbitrate the absent static target, which the bullet no longer forecloses: the three situations read one test at one moment rather than partitioning tests, so a static check whose file does not exist yet is in the first situation while that holds and in the third once the file is present. Resolve by what is true when the question is asked, not by the test's history.
- [x] 1.13 Amend the closing paragraph of that section — *"Rules that presuppose an absent target … bind only in the first situation. Everything else here binds in both"* — to say all three rather than both.

## 2. Amend `skills/testing/SKILL.md` — the fourth failure state

- [x] 2.1 In *What a failing test establishes*, amend the sentence introducing the enumeration. Its routing clause is sound, but its condition is the target's presence — *"where the target already exists"* — and presence now spans situations two and three, which read the second and fourth states differently from each other. Condition on the situation instead. An earlier draft of this change confirmed only that the section's name was unchanged, which is not the property that matters here.
- [x] 2.2 Add the fourth state's third-situation reading: where the assertion does not execute the behaviour it asserts and the target already carries that property, the check passes on its first run for that reason alone, so the alarm fires on every such check — sound ones included — and discriminates among none of them. Say the pass is answered by the fixture obligation rather than by investigating the pass itself, and say that a check red at authoring does not raise the fourth state at all. Put this beside the enumeration, not inside the fourth bullet: the bullet states what the state establishes, and this states when the state stops discriminating.
- [x] 2.3 Amend both two-way phrases in that section, not one. The third state's *"in either situation"* and the introducing paragraph's closing *"the third is the same either way"* are each written for two situations; both are situation-independent claims whose count is wrong. Task 2.1 amends the condition in that same sentence; this amends its closing clause. Apply both — they touch different halves of one sentence.

## 3. Amend `skills/testing/SKILL.md` — the bound on a discriminator

- [x] 3.1 In *Specified, derived, deliberately untested*, add what a fixture-driven discriminator is for: to establish that the check can fail, by falsifying it on the property the check exists to assert. Add only this — the delta carries the requirement's existing paragraphs and scenarios through verbatim, and an edit to any of them is outside what this change announced.
- [x] 3.2 State that this is not an enumeration of the check's input space, and that a case which falsifies nothing the existing cases do not already falsify is a derived assertion like any other and is labelled accordingly.
- [x] 3.3 State no number, proportion or size, and say why in one clause: the bound is what a case establishes, which is reviewable, rather than how many there are, which is not. Do not add a separate rule prohibiting over-large discriminators — Decision 2 in `design.md` records why that home was rejected.
- [x] 3.4 Check the section still reads as one argument after the insertion. The three classifications come first, the reason for labelling second; the discriminator bound is an application of the second and belongs after it, not between them.

## 4. Verify

- [x] 4.1 Run `bash tests/run.sh` from the repository root — this repository's suite is `tests/cases/*.sh` driven by that script, not a Python runner. Twenty-six of its cases name a path under `$TOOLKIT_ROOT/agents` or `$TOOLKIT_ROOT/rules` — that is the counting criterion, and it is stated because three plausible readings of "reads" give three different figures over this tree — so a case can break on an edit to an asset even though this change edits no `scripts/` behaviour. Record the result rather than inferring it. **Recorded: 55 passed, 0 failed** (baseline at the plan commit `2dcdca6`: 51 passed, 0 failed; the four derived cases were red at authoring and are green here). `openspec validate state-the-committed-artifact-situation --strict` **exits 0**, which is task 4.11's result and is recorded here so both figures sit together.
- [x] 4.2 State what this change's own scenarios are verifiable by, because task 4.1 does not cover them: no case under `tests/cases/` reads `skills/`, so the delta's scenarios have no existing test surface at all. Task 4.1 is a regression run over the twenty-six cases that read `agents/` or `rules/`, not coverage of this delta. Weigh the one recorded convention bearing on it: `AGENTS.md` says `tests/` holds a harness **exercising `scripts/`**, which read literally answers "none owed" — while twenty-six cases already read `agents/` or `rules/` statically, so the convention line has drifted from the practice. Record either that a new case reading `skills/testing/SKILL.md` is owed, or that none is and why, naming that drift either way.

  **Determined:** a case **is** owed, and four were written — `testing-skill-three-situations.sh`, `-discriminator-obligation-placement.sh`, `-failure-states-situation-conditioned.sh`, `-discriminator-bound.sh`. The practice governs over the stale `AGENTS.md` line; correcting that line is outside this change's announced `Impact`, and **is now queued** under *The suite's own gaps* in `docs/change-queue.md` — recorded there rather than only here, because a note kept inside a change is archived with it. Stated because the derived-test step will otherwise meet this gap with no warning. (`tools-passthrough.sh` mentions `skills` but asserts a generated `.agents/skills/` tree, not the library's, so the "no case reads `skills/`" claim is one grep away from a false positive.)
- [x] 4.3 Determine whether the fixture obligation is overridable by a consuming project's conventions, and record the determination here. *Consuming Project Conventions Take Precedence, Above a Stated Floor* names exactly two non-negotiable rules and says everything else defers in the ordinary way — so the rendered skill will carry "SHALL NOT present it as optional rigour" in one section and a deference rule in another. It defers in the ordinary way: it is not one of the two, and a project whose suite is green for reasons this floor cannot see is exactly the case that rule exists for. Recorded rather than asserted, because the change would otherwise never say whether joining the non-negotiables was considered.
- [x] 4.4 Re-read `skills/testing/SKILL.md` end to end for the claim in its own opening — that it is a floor, carrying nothing version-dependent and no framework specifics. The additions name no runner, no language and no fixture mechanism; confirm that holds as written rather than as intended.
- [x] 4.5 Leave *Reviewing or debugging rather than writing* unedited, and record the determination **here in this task** rather than in the section — editing it to explain why it was not edited is the contradiction it would create. The reason: the debug entry already routes a test that passes when it should not to the fourth state, and task 2.2 gives that state the third-situation reading and the route to the fixture obligation. The section therefore reaches the new material through the vocabulary it already names. An edit there would also be an asset change outside what this change announced, governed by a requirement the deltas do not modify.
- [x] 4.6 Extend that determination to the **review** entry as well as the debug one. *The Floor States How It Applies on a Review or Debug Entry* obliges a rule presupposing an authoring pass to be marked as such, and the skill's marking site is the "what does not apply on these entries" list. The change makes **two** additions and only one is authoring-scoped. The fixture obligation presupposes an authoring-or-modifying pass and is not added to that list: the scope rule already says it attaches to a check as authored or modified, which tells a reviewer it is not theirs to discharge. The discriminator bound is different — it lands inside the classification rule, which *The Floor States How It Applies on a Review or Debug Entry* names as **the** review question, so it applies on a review entry and reaches one through the sentence already there. Recorded because reasoning about the debug entry alone leaves the requirement's other half unanswered.
- [x] 4.7 Record one further determination, in this task: *An absent target is not a problem to fix* is left unedited. Task 1.12 routes a static check whose file does not exist into the first situation, where that section binds, and its enumeration is code-shaped — "the missing module, function, type, or an empty stub" — so it reaches a missing workflow or conventions file only by analogy. It is left as it stands because the prohibition's motive clause, which is about creating a target in order to make tests execute, is not code-specific and covers the case. Recorded rather than edited, for the reason task 4.5 gives.
- [x] 4.8 Record the determination on the frontmatter `description` too, in this task: it enumerates the body's topics and will not name the third situation, and it is left unchanged. The change adds no trigger surface, and `skill-authoring` invalidates a skill's recorded trigger fixtures on a change to its `name` or `description` — so editing it to be exhaustive would cost a re-verification for no routing gain. This is the one site of `SKILL.md` the other determinations do not reach.
- [x] 4.9 Confirm the skill's *Trigger check fixtures* section needs no new fixture, and record that determination **here, in this task**, not in the section. This change adds no trigger surface: the third situation is reached by an ordinary test-writing request, which the existing positive fixtures already cover. The section is a fourth `SKILL.md` section and `proposal.md`'s *Impact* announces three; writing into it would be the same unannounced asset edit that cost this change a review round, and it is governed by a `create-skill` requirement these deltas do not modify. Tasks 4.5 and 4.6 record their determinations the same way, and the symmetry is deliberate.
- [x] 4.10 Measure the rendered body against `create-skill`'s two size criteria rather than against this change's estimate of them: the loading budget (~5,000 words) and "readable in one pass". The situations section roughly triples, and `design.md` Decision 3's figure was written before three review rounds grew the delta. Re-count its "under a thousand words" estimate while measuring: it is unverified, and the delta's own normative prose now runs to roughly 1,600 words of specification text, which skill prose compresses from by an unknown factor.

  **Measured, 2026-09-11, after implementing, both criteria and by stated method.**

  *Budget.* Body after the frontmatter — the quantity `create-skill` puts the budget on — **4,148 words** against ~5,000, so **83%**. Whole file is 4,365; the earlier record gave the whole-file figure against a body budget, which was the wrong quantity for the comparison even though it changed no conclusion. Met, narrowly.

  *Readable in one pass.* Measured rather than asserted, because an earlier draft of this record asserted it. `## Which situation you are in` grew **187 → 1,254 words**, which is **6.5×**, not the "roughly triples" Decision 3 predicted — and 2× the next-largest section, flat, with no internal structure. That strained the criterion. Remedied **in place**: 7 `###` subsections, longest unbroken run now 229 words. Not a split into `references/` and not a trim, so Decision 3's single-flat-file conclusion stands on both criteria rather than being reopened — but it stands on a remedy, not on the prediction it made. Its "under a thousand words of normative material" claim — stated as a bound, not an estimate — was **breached**: body grew 2,887 → **4,199**, so **1,312 words**. Re-measured after the code review's restructuring, which added subsection headings; the figure recorded before it was 4,148. Both figures are body-after-frontmatter, the quantity the budget is on; an earlier draft of this very record gave whole-file numbers here while disowning that substitution one paragraph above. Its conclusion — that the budget is not the constraint deciding the single-flat-file question — survives; its margin claim does not, and the next change to this skill should treat the budget as live rather than distant. Where either criterion **fails**, Decision 3 is reopened before the skill is committed — it concluded a single flat file on both criteria, and a failure falsifies that conclusion rather than merely straining it; say then whether the remedy is a split into `references/` or a trim. Where either is met only narrowly, say so in the change's record — the decision to keep this in one flat file turns on both, and a margin claim nobody re-measured is the kind of figure a later reader falsifies by counting.
- [x] 4.11 Run `openspec validate state-the-committed-artifact-situation`.
- [x] 4.12 **Not dischargeable here — see *Discharged at the archive commit* below, which carries it in prose.** The archive-time confirmation could not be a ticked box in the file that contains it, so it is recorded there instead; this entry is ticked because recording it *is* what this task asks for.

## 5. Record what this change does not do
- [x] 5.1 **Done: the entry still holds as written; no correction owed.** Confirm the `share-a-suites-test-arrangement` entry in `docs/change-queue.md` still states what this change deferred, and correct it where the artifacts settled something differently. The entry was written when this change was drafted rather than at its end, so that it survives the change being abandoned.
- [x] 5.2 **Done: the opening claim was overstated and is corrected in the entry — the floor states three of the four obligations outright, and the fourth is reachable only by inference through *deliberately untested*.** Confirm the same of `record-the-discriminator-obligation-in-the-manifest`, the larger deferral: that it still names all four manifest obligations, and that the two findings it preserves — key the exception on the check's state, and keep the unrunnable record out of the baseline entry — still read as this change's artifacts settled them. Task 1.9 places that record on the classification surface, which the entry must not contradict. Test its opening claim too — *"The floor states all four obligations already"*: of the four, a check the pass leaves without a discriminator is reached only by inference through *deliberately untested*, and this change's delta states no record for it. Soften the sentence or point it at that classification, rather than letting a deferred change start from a premise these artifacts do not carry.

## Discharged at the archive commit

**Performed at the archive commit, 2026-09-11.** Check 2 passed — the requirement appears once as a `### Requirement:` header, under its new title, and the old title appears nowhere; no stale duplicate to delete. Check 3 passed — the discriminator paragraph sits in the classification requirement's body above its first scenario, where the delta places it. **Check 1 failed and was fixed in this commit**: the capability's *Purpose* enumerated four failure modes and named none for this change's, so "a suite green because nothing it asserts is ever executed" was added there. That is the check the third code review caught being silently dropped when this task moved from a ticked box into prose; had it stayed dropped, the omission would have been permanent from this commit onward.


- 4.12 — three archive-time checks over the merged `openspec/specs/testing-practice/spec.md`. **First**, that the capability's *Purpose* paragraph names the failure mode this change adds — a suite green and asserting nothing; it enumerates four today and does not, and the omission is permanent from the archive commit onward, so add it in the same commit. **Second**, that the renamed requirement appears exactly once, under its new title, carrying the delta's body; delete a stale duplicate in the same commit. **Third**, that the discriminator paragraph sits in the classification requirement's body, above its scenarios; restore it there if a merge relocated it. The first was dropped when this task moved from a ticked box into prose, and the third code review caught it — it is restored here because prose is where it now lives.
  Reason: it discharges at the archive commit, which happens after the commit that writes this file, so it cannot be ticked here without the tick meaning something it does not. It is stated here in prose instead, which is where a task that outlives its own task list belongs.
