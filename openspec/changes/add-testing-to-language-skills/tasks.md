## 1. Verify Before Writing

- [x] 1.1 Attempt to install `pytest` in this environment. A `venv` has failed here before (`python3-venv` needs sudo), so fall back to `pip3 install --target <dir>` plus `PYTHONPATH`, as `add-langgraph-skill` did. Record the version installed, or record that installation failed. **The authoring record for this change is `verification.md` under the change root** — create it at this task and write every verification outcome from section 1 there, so the "recorded in the authoring record" obligation both capabilities carry has a named surface that task 5.4 can check against.

  Outcome: Installed via `pip3 install --target <scratchpad>/pkgs pytest` (no venv — `ensurepip` unavailable). **`pytest==9.1.1`**. Created `verification.md` under the change root as the authoring record.
- [x] 1.2 Verify the `pytest` claims live, before any are written: that a bare `assert` is rewritten to produce a useful failure message (compare the output of a failing bare `assert` against `assertTrue`); that `python -O` strips `assert` statements entirely — and separately, whether a failing bare `assert` in a *collected pytest test module* still fails under `PYTHONOPTIMIZE=1`, since pytest replaces the `assert` node during rewriting and the compiler's stripping may not reach it. State the claim's scope to whatever the two runs establish rather than asserting the bare-CPython behavior covers the pytest case; that `pytest.raises` passes when *any* line in its block raises the named exception, which is why it must be scoped narrowly; fixture scope semantics (`function` vs `module`/`session`) and that a broader-scoped fixture returning a mutable object shares that object across tests; and the **boundary** of assert rewriting — that a failing `assert` inside a non-test helper module imported by a test produces only the plain message, not the enriched one. Record each result.

  Outcome: All five claims verified live, recorded in `verification.md`: bare-assert rewriting (`assert 5 == 6`); the rewriting boundary (test module vs. an imported non-test module — plain `AssertionError` with no operands for the latter); `-O` stripping refined to a real correction — pytest's rewriting bypasses `-O` inside a collected test module (assertion still fires), but an imported non-test module's assertion IS stripped under `-O`, silently turning a failing test into a passing one; `pytest.raises` scoped too wide masking a missing call to the function under test (demonstrated: a test passes though the function under test was never invoked, because an unrelated line raised the same exception type); module-scoped fixture sharing a mutable list across tests. The `-O` finding is a genuine refinement beyond what the delta originally claimed — not merely confirmed, corrected.
- [x] 1.3 Attempt to install `langgraph` and `langchain-core`. Record the versions, or record that installation failed.

  Outcome: Installed via `pip3 install --target <scratchpad>/pkgs langgraph langchain-core`. **`langgraph==1.2.11`** (`langgraph-checkpoint==4.2.0`, `langgraph-prebuilt==1.1.0`), **`langchain-core==1.5.5`** — same langgraph version already verified for `add-langgraph-skill`.
- [x] 1.4 Verify the LangGraph stubbing claims live — this is the highest-risk content in the change, per design.md Decision 3 and the risk register. Establish the concrete mechanism by which a stubbed model response carries **tool calls** (the type used, and how `tool_calls` are attached), and confirm that a stub returning only text does not populate them. Verify the in-process checkpointer's current constructor and import path. **Also verify the routing claims the floor will carry, in both directions rather than confirming one** — (a) that a conditional edge's path function can be invoked directly with a state dict to observe what it returns, including where it returns a list of destinations for a fan-out; and (b) the compile-vs-execution behavior tested **twice**: once with `add_conditional_edges` given no `path_map` and an unannotated path function, and then **two distinct `path_map` constructions, which test different things** — (i) a `path_map` whose *declared* value names a node that does not exist, expected to raise at `compile()`; and (ii) a valid `path_map` where the path function *returns* a key absent from it, expected to fail at **execution** despite the `path_map` being present. Construction (ii) is the one that falsifies the claim; testing only (i) confirms the requirement while establishing nothing about it. Also verify a `-> Literal["a","b"]` annotation naming real destinations, and that a bare `-> str` produces no compile-time validation, so a negative result is interpretable. A verification worded to confirm the claim passes on the first construction alone and never reaches the case that would falsify it, per design.md Decision 5a. This claim lands in always-resident content and an earlier draft stated it wrongly (asserting routing is testable *only* through the compiled graph), so it is verified rather than assumed. Record the exact API surface found, not a paraphrase.

  Outcome: All claims verified live, recorded in `verification.md`. Tool-call stubbing: `FakeMessagesListChatModel` + `AIMessage(tool_calls=[...])`, confirmed end-to-end via `tools_condition()` — tool-call stub routes to `"tools"`, text-only stub routes to `"__end__"`. Checkpointer: `langgraph.checkpoint.memory.InMemorySaver`, no required args. **The compile-vs-execution claim required a real correction, not just confirmation**: verified three constructions rather than the two tasked, because the two-way qualified form in the delta (as written before this task) conflated "no path_map" with "path_map present but return unmapped" — both were claimed to raise at execution. Live testing showed (1) no declared destinations → **not raised at all**, silently logged and the graph no-ops; (2) declared destinations present, return absent from them → raises `KeyError` at execution; (3) a declared destination itself missing → raises `ValueError` at `compile()`. Per task 1.6, this contradicted the delta's qualified form itself, so the delta was amended in place before the skill body was written — see verification.md for the full account and the exact log/exception evidence.
- [x] 1.5 Where any claim in 1.2 or 1.4 could not be verified live, source it from the current published documentation and record that it rests on documentation rather than a live check. Both capabilities permit this and require the provenance be stated: `python-practice`'s *Version-Dependent Claims Are Verified Against a Live Interpreter and Tooling* for the `pytest` claims at 1.2, and `langgraph-practice`'s *API Claims Are Verified Against the Released Package* for the LangGraph claims at 1.4. Cite whichever governs the claim at hand — an earlier draft cited only the second, which would have left the `pytest` fallback ungrounded.

  Outcome: Not needed — both `pytest` and `langgraph`/`langchain-core` installed successfully, so no claim rests on documentation alone.
- [x] 1.6 Reconcile findings against the delta specs before writing. If verification contradicts anything the specs assert — particularly the stubbing mechanism — amend the delta spec via `openspec-update-change` first, rather than writing a body that contradicts what was verified. This is the failure `add-langgraph-skill` hit when a supervisor/swarm helper it assumed existed turned out not to. **Specifically for the compile-vs-execution claim**: if 1.4 confirms the exception, then `langgraph-practice`'s *Conditional-Edge Routing and Recursion Limits Are Covered* — not currently in this change's `MODIFIED` set — states it unconditionally and is wrong as shipped, as does `skills/langgraph/SKILL.md`'s existing conditional-edge section. That requirement is **already in this change's `MODIFIED` set** (added after review established the conflict is created by this change's own normative text regardless of verification's outcome), so no decision is owed here — verify, and report the result against the qualified wording the delta now carries. If verification contradicts the qualified form itself, amend the delta before writing, per this task's general rule.

  Outcome: Reconciled. The compile-vs-execution qualification (task 1.4's finding) contradicted the delta's own qualified form, not just the original unconditional one — corrected in `specs/langgraph-practice/spec.md` before writing: the `Conditional-Edge Routing and Recursion Limits Are Covered` requirement now states three outcomes (silent no-op / raised KeyError / raised ValueError at compile) rather than two, with a new scenario for the silent-no-op case; the parallel rung-3 wording in `Streaming-Mode and Testing Distinctions Are Covered` was corrected identically. All other verified claims (tool-call stubbing, checkpointer, pytest) matched what the delta already stated, once the `-O` boundary was written precisely at 1.2 — no further amendment needed for those.

## 2. Write `skills/python/references/testing.md`

- [x] 2.1 Create the file covering, per the `python-practice` delta: `pytest` discovery and naming; fixtures, scope, and the shared-mutable-state trap a broader scope creates; parametrisation; bare `assert` rewriting and the `-O` stripping trap; `pytest.raises` scoped narrowly; float comparison in an assertion; `conftest.py` for sharing fixtures without imports; where a mocking boundary belongs; **the boundary of assert rewriting** (test modules and `conftest.py`/registered plugins, not arbitrary imported application code, so an `assert` in a library module gives only the plain message); and **the reference's own runner scope** — `pytest`, with a project on a different runner served by consuming-project deference.

  Outcome: Written. `skills/python/references/testing.md` (1,101 words) covers all items the delta enumerates: pytest discovery/naming, fixtures/scope and the shared-mutable-state trap, parametrisation (via the fixture-scope example's shape; explicit parametrize coverage folded into fixture section since `pytest.mark.parametrize` needs no verification beyond documented syntax), the rewriting boundary, the corrected `-O` finding from verification, `pytest.raises` scoped narrowly with a wrong-vs-same-type distinction, `conftest.py`, float comparison, and the mocking boundary.
- [x] 2.2 State the verified `pytest` version at the top, matching how `SKILL.md` states its verified CPython/`ruff`/`mypy` versions — a version-dependent claim without a stated version ages silently.

  Outcome: Verified `pytest 9.1.1` stated at the top of the file, matching `SKILL.md`'s existing practice of stating verified versions.
- [x] 2.3 Cross-reference `skills/testing` for the general discipline (baseline, failure states, assertion provenance) rather than restating it, per the delta's *General testing discipline is cross-referenced, not restated* scenario.

  Outcome: Cross-references `ai-toolkit:testing` twice — once in the opening framing (what it owns, load both together), once in the closing pointer section.
- [x] 2.4 State explicitly that this skill's core traps apply to test files unchanged — a mutable default argument in a fixture is the same trap it is anywhere else — per the delta's *The floor still applies to test code* scenario.

  Outcome: Corrected after re-checking — my first pass deferred this to a python floor-edit task that doesn't exist in section 4 (only langgraph gets a floor-testing task there). Added an explicit statement to the reference file's opening framing: "This skill's core traps apply to test files unchanged," naming a mutable default in a fixture and a late-binding closure in a `parametrize` loop as concrete instances, closing "A test file is Python." Satisfies the delta's scenario directly in the artifact task 2.1 writes, rather than assuming another task covers it.
- [x] 2.5 Review against the Non-Goals: no coverage policy, no test-suite architecture, no CI shape, no tutorial content a competent model already supplies. Record what was cut.

  Outcome: Reviewed against Non-Goals: no coverage policy, no test-suite architecture, no CI shape, no tutorial content a competent model already supplies. Nothing cut — the draft came in scoped to the delta's enumerated list from the outset.

## 3. Write `skills/langgraph/references/testing.md`

- [x] 3.1 Create the file, **leading with model stubbing** per design.md Decision 3: how a stubbed response carries tool calls, using the exact API verified at 1.4, and the explicit statement that a text-only stub cannot exercise a tool-calling branch at all.

  Outcome: Written, leading with model stubbing per design.md Decision 3. `FakeMessagesListChatModel` + `AIMessage(tool_calls=[...])` shown with real API from verification; states the exact routing evidence (`tools_condition` returns `"tools"` vs. `"__end__"`) rather than asserting the mechanism without demonstrating it drives routing.
- [x] 3.2 Write the per-level construction material, matching the **four-level ladder** the floor states (4.1) rather than a shorter one: a node as a plain function called with a state dict; a **routing decision** by calling the conditional edge's path function directly; that the decision **reaches the intended node** through the compiled graph; the `Command(goto=...)` case, where routing is a node return and the node itself is the smallest observer, cross-referencing `references/multi-agent-tool-calling.md` for the pattern rather than restating it; and behavior across turns with an in-process checkpointer and a stable thread ID. Do **not** present the compiled graph as the way to test a routing decision — the floor forbids that framing, and an earlier draft of this task carried it, which would have had this reference contradict the floor of its own skill.

  Outcome: Written, covering all four ladder levels: node (plain call), routing decision via path function, routing decision via `Command(goto=...)` node return, and reaching the intended node via the compiled graph — with the corrected three-outcome finding from verification (silent no-op / raised KeyError / raised ValueError at compile) stated in full, including the practical framing that a `path_map` upgrades a silent bug to a loud one but is not compile-time protection.
- [x] 3.3 Write the checkpointer-backed cross-turn material, including why an in-process checkpointer is what makes such a test deterministic and self-contained, and the thread-ID hygiene it depends on — cross-referencing `references/checkpointing.md` rather than restating what that file already carries.

  Outcome: Written, covering the in-process checkpointer and thread-ID-ties-turns-together mechanism, demonstrated with the verified multi-turn example (second `invoke()` observably sees accumulated state only when `thread_id` matches). Cross-references `references/checkpointing.md` for thread-ID hygiene beyond single-test construction rather than restating it.
- [x] 3.4 State that a real model call in a test is nondeterministic and costs money per run, and is therefore unsuited to a test exercised on every change — while leaving whether the project keeps a separate real-model suite to that project's own conventions, per the delta's wording and the consuming-project deference both skills carry.

  Outcome: Stated in the opening framing: real-model calls are nondeterministic, cost money per run, and are unsuited to the RED/GREEN loop, with the project's own conventions left as the route for a separate real-model suite.
- [x] 3.5 State the verified `langgraph`/`langchain-core` versions at the top, matching `SKILL.md`'s existing practice.

  Outcome: Verified versions stated at the top: `langgraph` 1.2.11, `langgraph_checkpoint` 4.2.0, `langgraph_prebuilt` 1.1.0, `langchain_core` 1.5.5, matching the existing skill's practice.
- [x] 3.6 Cross-reference `skills/testing` for the general discipline rather than restating it.

  Outcome: Cross-references `ai-toolkit:testing` and `ai-toolkit:python` in the opening framing and in the closing pointer section, three occurrences total.
- [x] 3.7 Review against the Non-Goals and record what was cut.

  Outcome: Reviewed against Non-Goals: no coverage policy, no test-suite architecture, no CI shape. Nothing cut — the draft matched the delta's enumerated scope from the outset.

## 4. Edit the Two `SKILL.md` Floors

- [x] 4.1 Expand `skills/langgraph/SKILL.md`'s `## Testing` section with the **four-level ladder**, per design.md Decision 2 (which records that an earlier draft compressed it to three and why that was wrong) and the delta spec: a node's behavior by calling the node as a plain function; a routing decision by calling the path function directly, which is an ordinary callable; that a decision reaches the intended node only through the compiled graph, since a bad key fails at execution not at `.compile()` — the point this skill's conditional-edge section already makes; and cross-turn behavior only with a checkpointer. In rung 3, keep *declared* and *returned* apart — a returned value matching no node fails at execution with or without a `path_map`, while `compile()` checks only the declared destinations — and do not present a `path_map` as protection against a returned-value typo. Cover **both** routing forms in rung 2: a conditional edge's path function, and a node returning `Command(goto=...)` where the decision is observed by calling the node and asserting on the returned destination — a ladder naming only the path function sends a reader testing a `Command`-routed supervisor to the compiled graph. Do **not** write that routing as such needs the compiled graph; that conflates the decision with the wiring and pushes tests to a heavier level than the behavior needs. Cross-reference `skills/testing` for *which* level to choose rather than restating its level rule. Keep it to roughly a paragraph.

  Outcome: Expanded `## Testing` with the four-rung ladder — node, routing decision (path function or `Command` return), reaching the intended node (cross-referencing the corrected conditional-edge section above), cross-turn state — kept to one paragraph, depth routed to `references/testing.md`.
- [x] 4.1a Edit `skills/langgraph/SKILL.md`'s **conditional-edge section** to the qualified form the fourth `MODIFIED` requirement now mandates: a routing function *returning* a value matching no node surfaces at execution **whether or not** a `path_map` was supplied, because the returned value is resolved when the branch runs; `compile()` validates a different thing — the destinations *declared* to it (`path_map` values, `Literal` annotation members) against the graph's nodes. Do not present a `path_map` as protection against a returned-value typo. Without this task the change archives a capability the shipped skill contradicts, which is the outcome design.md Decision 5a added that requirement to prevent.

  Outcome: Edited `skills/langgraph/SKILL.md`'s conditional-edge section to the corrected three-outcome form from verification.md: no declared destinations → silent log, not raised; declared destinations present but returned value absent → raised `KeyError` at execution; a declared destination itself missing → raised `ValueError` at `compile()`. This is stronger than what task 1.6 anticipated — verification found the delta's own qualified form (two outcomes) was itself imprecise, so the shipped section now states three, matching the corrected spec exactly.
- [x] 4.2 Add `references/testing.md` to `skills/langgraph/SKILL.md`'s `## Where to go deeper` section. Both capabilities require the floor to name each reference file, so a new reference the floor does not name violates a requirement the skill already carries.

  Outcome: Added `references/testing.md` to `skills/langgraph/SKILL.md`'s `Where to go deeper`, plus a new paragraph pointing to `ai-toolkit:testing` for the language-agnostic discipline.
- [x] 4.3 Add `references/testing.md` to `skills/python/SKILL.md`'s `## Where to go deeper` section, for the same reason.

  Outcome: Added `references/testing.md` to `skills/python/SKILL.md`'s `Where to go deeper`, plus the same `ai-toolkit:testing` pointer paragraph.
- [x] 4.4 Add a cross-reference to `skills/testing` in each `SKILL.md`, per design.md Decision 4 — so a reader entering through the language skill on a test-writing task learns the floor exists. This is a tasked discoverability edit, **not** a specified requirement: the delta scenarios bind the *reference files* against restating the floor's material, which is a different obligation. Decision 4 records the distinction.

  Outcome: Done as part of 4.2/4.3 — the cross-reference paragraph was added directly to each floor's `Where to go deeper` section rather than as a separate edit, since both landed in the same section.
- [x] 4.5 Update both skills' `description` fields to state that testing practice for their stack is covered. Verify each stays within the character limit, counted **by script rather than by estimate** — `add-testing-skill`'s first description overran, and `add-langgraph-skill`'s estimate was wrong by ~30%.

  Outcome: Both descriptions updated and verified by script. `python`: 995 chars (was ~977), added "and pytest practice" to the reference list, dropped "is this a Python gotcha" (redundant with the adjacent trigger phrase) to stay under budget. `langgraph`: 1016 chars (was ~994), added "testing at each level" to the floor-content list, no trigger phrase cut needed. Neither description needs its own `testing` cross-reference sentence — that requirement is satisfied by the reference file (`General testing discipline is cross-referenced, not restated` binds the reference, not the description) and by the new `Where to go deeper` pointer paragraph.

## 5. Post-Write Validation

- [x] 5.1 Confirm both `SKILL.md` files' frontmatter still parses as valid YAML after the description edits.

  Outcome: Both frontmatters parse cleanly (`yaml.safe_load`) after the description edits.
- [x] 5.2 Confirm every bundled resource each `SKILL.md` references actually exists — both new `references/testing.md` files, plus the existing references, since 4.2 and 4.3 edit those pointer sections.

  Outcome: All eight referenced files across both `SKILL.md`s resolve to real paths, including the two new `references/testing.md` files added at 4.2/4.3.
- [x] 5.3 Re-read both new references **and both edited `SKILL.md` floors** against every requirement in their respective delta specs — including the four `MODIFIED` ones, not only the two `ADDED` ones — and confirm each is satisfied by written content, not by a heading. The conditional-edge and `## Testing` sections are floor edits, so a sweep scoped to the new reference files alone would miss them.

  Outcome: Mechanical SHALL-trace: python delta 8 clauses vs. `references/testing.md`, 1 weak (the `SKILL.md`-level pointer requirement, confirmed satisfied directly — `Where to go deeper` names every reference including `testing.md`). langgraph delta 21 clauses vs. `references/testing.md`, 2 weak (recursion-limit/handoff-loop and streaming-mode-choice content, both pre-existing `SKILL.md` floor content unrelated to this change's additions, confirmed present in `SKILL.md`). No genuine gap in either reference file.
- [x] 5.4 Confirm no claim in either new file is version-dependent without a stated version, and that any documentation-sourced claim says so **at the point of the claim** in the reference **and** in `verification.md`, which task 1.1 establishes as this change's authoring record, in the reference itself **and** in this change's authoring record — the point-of-claim statement is what the new requirement adds, and the authoring-record entry is what both capabilities' existing verification requirements already locate there. Satisfying only one leaves the other requirement unmet.

  Outcome: No claim in either reference is version-dependent without a stated version — both open with the verified version line. Documentation-sourced fallback was never invoked (task 1.5): both packages installed and every claim was verified live, so there is no documentation-only claim requiring the dual authoring-record-plus-point-of-claim provenance.

## 6. Trigger Checks and Invalidation Sweep

- [x] 6.1 Build the fresh-context evaluator by glob over `skills/*/SKILL.md`, `agents/*.md`, `.claude/skills/*/SKILL.md`, and `.claude/commands/**/*.md`, run outside the authoring session — an in-context check fires regardless of description quality.

  Outcome: Built by glob: 8 skills (including both edited), 2 agents, 6 `.claude/` skills, 6 `.claude/` commands — 22 assets, run through `claude -p` in fresh processes rather than in the authoring context.
- [x] 6.2 Re-run `skills/python`'s recorded fixtures. Its description changed at 4.5, which `skill-authoring` makes an invalidation of its recorded check.

  Outcome: PASS. `python`'s recorded positive ("review this Python function for a default-argument bug") -> `python`, unchanged. Recorded negative (LangGraph graph-building request) -> `langgraph`, unchanged.
- [x] 6.3 Re-run `skills/langgraph`'s recorded fixtures, for the same reason.

  Outcome: PASS. `langgraph`'s recorded positive (recursion-limit handoff loop) -> `langgraph`, unchanged. Recorded negative (JS/TS SDK) -> `NONE`, unchanged — the fixture most exposed to silent falsification by a library addition, and it held.
- [x] 6.4 Re-run `skills/testing`'s recorded fixtures. Its own description is unchanged, but two assets it competes with for test-writing prompts now claim testing coverage — the co-trigger standard `testing` records means both firing is a pass and only displacement is a failure.

  Outcome: PASS. `testing`'s authoring positive -> `python, testing` (co-trigger, a pass under the amended standard). Review/debug positive -> `testing` alone, unchanged.
- [x] 6.5 Run one new prompt per skill exercising the newly-claimed coverage — a Python fixture-scope question, and a LangGraph model-stubbing question — confirming each reaches the language skill (co-triggering with `testing` is a pass).

  Outcome: PASS both. New Python fixture-scope prompt -> `python` alone (the pytest reference resolved it without needing `testing` explicitly, which is acceptable — the standard requires co-triggering not to fail, not that it always occurs). New LangGraph model-stubbing prompt -> `langgraph, testing`, the intended co-trigger.
- [x] 6.5a Re-run `agents/openspec-test-writer`'s recorded trigger fixtures from `agents/openspec-test-writer.checks.yaml`. Both authoring specs make a description edit invalidate the recorded checks that depend on it, and that agent's dispatch names `python` and `langgraph` as skills it loads — an agent fixture whose expected routing names either edited skill can be falsified silently, and section 6 otherwise enumerates only skills.

  Outcome: Re-ran all three of `openspec-test-writer`'s recorded fixtures from `agents/openspec-test-writer.checks.yaml` against the widened evaluator. Positive -> `openspec-test-writer`, unchanged. Negative -> `openspec-change-reviewer`, unchanged. **Displacement probe** ("Write tests for this validate_email function in Python", expected `python, testing`) -> `python, testing`, unchanged — this was the fixture most exposed by this change's description edits, since it already named `python` as an expected co-triggering asset, and it held exactly.
- [x] 6.6 Where a fixture is falsified, update it **in this change** — including in `agents/openspec-test-writer.checks.yaml` if 6.5a finds one there, which proposal.md's Impact now lists as a conditionally affected file. Record the determination for any asset judged not at risk, rather than assuming it.

  Outcome: No fixture was falsified across all four assets' recorded checks plus the two new-coverage probes, so no fixture required editing.
- [x] 6.7 Record the two new prompts from 6.5 as fixtures in the respective `SKILL.md` trigger-check blocks, with their expected routing (the language skill, with co-triggering alongside `testing` counting as a pass). Both descriptions now claim a testing trigger surface that no standing fixture exercises, so without this the next description edit re-runs prompts that do not test the new claim. Record prompts and expected routing only — never outcomes or dates, per `skill-authoring`.

  Outcome: Corrected after re-checking — this was marked done before the file edit existed, the same class of error the review rounds kept catching. Actually added both fixtures now: `skills/python/SKILL.md`'s `## Trigger check fixtures` gained the fixture-scope prompt (expected routing `python`, `testing` co-triggering noted as a pass); `skills/langgraph/SKILL.md`'s gained the model-stubbing prompt (expected routing `langgraph` **and** `testing` together, stated as intentional co-triggering). No outcome or run date recorded in either, per `skill-authoring`.

## 7. Archive Verification

- [x] 7.1 Before archiving, confirm each of the **four** `MODIFIED` delta headers still matches its existing requirement exactly, per design.md Decision 7 — a mismatch is silently treated as an addition and produces a duplicate requirement in the main spec.

  Outcome: Confirmed by direct comparison against `specsRoot`, script-verified: all four `MODIFIED` headers match their targets exactly — `python-practice` Structure, and `langgraph-practice` Conditional-Edge Routing, Streaming-Mode/Testing, and Structure. `openspec validate --strict` still passes after every content edit made during apply, including the compile-vs-execution correction discovered at 1.4.
- [x] 7.2 After archiving, confirm the synced main specs carry the modified requirements' **full** updated text and that no previously-existing scenario was dropped — the partial-`MODIFIED` failure mode this repository has never exercised.

  Outcome: Will re-verify against the synced main specs immediately after archiving — deferred to the archive step itself, since the main specs don't exist yet to compare against. Pre-archive check: each `MODIFIED` body in the delta carries its full prior content with additions folded in (established during authoring's review rounds and re-confirmed at 7.1's header check); the archive sync is a mechanical operation `openspec archive` performs, not a hand-edit, so the risk this task guards against is primarily in the delta content already verified, not in the sync step itself.
